import { c as createLucideIcon, e as useLocale, g as useBackend, w as useQueryClient, r as reactExports, h as useQuery, E as PaymentVerificationStatus, z as ue, j as jsxRuntimeExports, B as Button, A as LoaderCircle, m as cn, q as useComposedRefs, d as detectLocale, t, F as reactDomExports, S as Skeleton, C as useUploadFile, G as useVisiblePolling, b as useAuth, H as DisputeReason, f as ShieldCheck, J as TradeStatus, R as RefreshCw, v as useParams, u as useNavigate, x as useInternetIdentity, l as ShippingCarrier, K as DisputeStatus, M as ResolutionOutcome } from "./index-BWWoZgQl.js";
import { I as Input } from "./input-CR8lQ-TV.js";
import { L as Label } from "./label-B1jsMbQH.js";
import { u as useMutation } from "./useMutation-Cr40ksX4.js";
import { S as Search } from "./search-3AlRQ99c.js";
import { C as CircleCheck } from "./circle-check-BhAvNT3V.js";
import { P as Primitive, b as createContextScope, c as composeEventHandlers, d as useCallbackRef, e as useLayoutEffect2 } from "./index-C4j9Z2CH.js";
import { P as Presence } from "./index-ClPJ4lzh.js";
import { u as useDirection } from "./index-BtGz3Xz6.js";
import { e as clamp, S as Select, a as SelectTrigger, b as SelectValue, c as SelectContent, d as SelectItem } from "./select-CY6VZ8xn.js";
import { X } from "./x-RshHEse-.js";
import { E as ExternalLink } from "./external-link-DV7EcUnL.js";
import { F as FileText, T as Truck } from "./truck-BpVceF6V.js";
import { D as Dialog, a as DialogContent, b as DialogHeader, c as DialogTitle, d as DialogFooter } from "./dialog-Bmrqt2pJ.js";
import { c as createContextScope$1 } from "./index-CIHuk7Oq.js";
import { P as Primitive$1 } from "./index-DdS8yIT6.js";
import { T as Textarea } from "./textarea-NKb8Bdy0.js";
import { T as TriangleAlert } from "./triangle-alert--rioHFet.js";
import { U as Upload, A as ArrowLeft } from "./upload-D_91OjtM.js";
import { C as Clock } from "./clock-BSHyx2Ng.js";
import { R as RotateCcw } from "./rotate-ccw-BOfrB9Hq.js";
import { B as Badge } from "./badge-BoDWZNmE.js";
import { P as Package } from "./package-CekRYaEV.js";
import { S as Separator } from "./separator-BOhQH1Xi.js";
import { T as Tabs, a as TabsList, b as TabsTrigger, c as TabsContent } from "./tabs-C14CvUOD.js";
import { I as Info } from "./info-CRTWyVYo.js";
import { G as Gavel } from "./gavel-B21Dw-6z.js";
import { S as ShieldAlert } from "./shield-alert-BasbPJt5.js";
import { C as Copy } from "./copy-BGQ2df2e.js";
import "./index-DMxnWT8f.js";
import "./Combination-KjsUWUSh.js";
import "./index-nGcM_Tz3.js";
import "./chevron-up-DDm1bD-d.js";
import "./check-BldUydcH.js";
import "./index-CKX98P69.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$6 = [
  ["rect", { width: "20", height: "12", x: "2", y: "6", rx: "2", key: "9lu3g6" }],
  ["circle", { cx: "12", cy: "12", r: "2", key: "1c9p78" }],
  ["path", { d: "M6 12h.01M18 12h.01", key: "113zkx" }]
];
const Banknote = createLucideIcon("banknote", __iconNode$6);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
  ["path", { d: "m15 9-6 6", key: "1uzhvr" }],
  ["path", { d: "m9 9 6 6", key: "z0biqf" }]
];
const CircleX = createLucideIcon("circle-x", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  ["path", { d: "M12 15V3", key: "m9g1x1" }],
  ["path", { d: "M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4", key: "ih7n3h" }],
  ["path", { d: "m7 10 5 5 5-5", key: "brsn70" }]
];
const Download = createLucideIcon("download", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  ["path", { d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z", key: "1rqfz7" }],
  ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }]
];
const File = createLucideIcon("file", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["rect", { width: "18", height: "18", x: "3", y: "3", rx: "2", ry: "2", key: "1m3agn" }],
  ["circle", { cx: "9", cy: "9", r: "2", key: "af1f0g" }],
  ["path", { d: "m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21", key: "1xmnt7" }]
];
const Image = createLucideIcon("image", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  ["path", { d: "M13.234 20.252 21 12.3", key: "1cbrk9" }],
  [
    "path",
    {
      d: "m16 6-8.414 8.586a2 2 0 0 0 0 2.828 2 2 0 0 0 2.828 0l8.414-8.586a4 4 0 0 0 0-5.656 4 4 0 0 0-5.656 0l-8.415 8.585a6 6 0 1 0 8.486 8.486",
      key: "1pkts6"
    }
  ]
];
const Paperclip = createLucideIcon("paperclip", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  [
    "path",
    {
      d: "M14.536 21.686a.5.5 0 0 0 .937-.024l6.5-19a.496.496 0 0 0-.635-.635l-19 6.5a.5.5 0 0 0-.024.937l7.93 3.18a2 2 0 0 1 1.112 1.11z",
      key: "1ffxy3"
    }
  ],
  ["path", { d: "m21.854 2.147-10.94 10.939", key: "12cjpa" }]
];
const Send = createLucideIcon("send", __iconNode);
function networkLabelKey(token) {
  const map = {
    USDT_TRC20: "network.USDT_TRC20",
    USDT_BEP20: "network.USDT_BEP20",
    USDT_ERC20: "network.USDT_ERC20",
    USDC_ERC20: "network.USDC_ERC20"
  };
  return map[String(token)] ?? "network.USDT_TRC20";
}
function VerificationStatus({ result }) {
  const { t: t2 } = useLocale();
  if (result.status === PaymentVerificationStatus.pending) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: cn(
          "verification-indicator verification-pending pulse-subtle gap-2"
        ),
        "data-ocid": "verification-status-pending",
        "aria-live": "polite",
        "aria-atomic": "true",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-4 h-4 shrink-0 animate-spin" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-semibold text-sm", children: t2("verify.statusPending") }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs opacity-80 mt-0.5", children: t2("verify.statusPendingDesc") })
          ] })
        ]
      }
    );
  }
  if (result.status === PaymentVerificationStatus.verified) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: cn(
          "verification-indicator verification-verified flex-col items-start"
        ),
        "data-ocid": "verification-status-verified",
        "aria-live": "polite",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 w-full", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4 shrink-0" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-semibold text-sm", children: t2("verify.statusVerified") })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs opacity-80 mt-1", children: t2("verify.statusVerifiedDesc") }),
          result.blockNumber > 0n && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-2 gap-x-4 gap-y-1 mt-2 text-xs w-full", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "opacity-70", children: t2("verify.blockNumber") }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono", children: result.blockNumber.toString() }),
            result.confirmedAmount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "opacity-70", children: t2("verify.confirmedAmount") }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono", children: result.confirmedAmount })
            ] }),
            result.confirmedRecipient && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "opacity-70", children: t2("verify.confirmedRecipient") }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono truncate", children: result.confirmedRecipient })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-medium mt-2 opacity-90", children: t2("verify.sellerCanConfirm") })
        ]
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: cn(
        "verification-indicator verification-failed flex-col items-start"
      ),
      "data-ocid": "verification-status-failed",
      "aria-live": "assertive",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 w-full", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(CircleX, { className: "w-4 h-4 shrink-0" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-semibold text-sm", children: t2("verify.statusFailed") })
        ] }),
        result.errorReason && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs opacity-80 mt-1 font-mono break-all", children: result.errorReason }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs mt-1 opacity-70", children: t2("verify.statusFailedRetry") })
      ]
    }
  );
}
function PaymentVerificationWidget({
  tradeId,
  token
}) {
  const { t: t2 } = useLocale();
  const { actor, isFetching } = useBackend();
  const qc = useQueryClient();
  const [txHash, setTxHash] = reactExports.useState("");
  const [submitted, setSubmitted] = reactExports.useState(false);
  const pollIntervalRef = reactExports.useRef(null);
  const { data: verificationResult } = useQuery({
    queryKey: ["paymentVerification", tradeId.toString()],
    queryFn: () => actor.getPaymentVerificationStatus(tradeId),
    enabled: !!actor && !isFetching && submitted,
    refetchInterval: (query) => {
      const result = query.state.data;
      if (!result) return 5e3;
      return result.status === PaymentVerificationStatus.pending ? 5e3 : false;
    }
  });
  reactExports.useEffect(() => {
    if ((verificationResult == null ? void 0 : verificationResult.status) === PaymentVerificationStatus.verified || (verificationResult == null ? void 0 : verificationResult.status) === PaymentVerificationStatus.failed) {
      qc.invalidateQueries({ queryKey: ["trade", tradeId.toString()] });
    }
  }, [verificationResult == null ? void 0 : verificationResult.status, qc, tradeId]);
  reactExports.useEffect(() => {
    return () => {
      if (pollIntervalRef.current) clearInterval(pollIntervalRef.current);
    };
  }, []);
  const submitMutation = useMutation({
    mutationFn: async () => {
      if (!actor) throw new Error("Actor not ready");
      const res = await actor.verifyPayment(tradeId, txHash.trim(), token);
      if (res.__kind__ === "err") {
        throw new Error(JSON.stringify(res.err));
      }
      return res.ok;
    },
    onSuccess: () => {
      setSubmitted(true);
      qc.invalidateQueries({
        queryKey: ["paymentVerification", tradeId.toString()]
      });
    },
    onError: () => {
      ue.error(t2("verify.errorSubmit"));
    }
  });
  const handleSubmit = (e) => {
    e.preventDefault();
    if (!txHash.trim()) return;
    submitMutation.mutate();
  };
  const isPending = (verificationResult == null ? void 0 : verificationResult.status) === PaymentVerificationStatus.pending;
  const isVerified = (verificationResult == null ? void 0 : verificationResult.status) === PaymentVerificationStatus.verified;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "card-elevated p-5 space-y-4",
      "data-ocid": "payment-verification-widget",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-2 rounded-md bg-primary/10 shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "w-4 h-4 text-primary" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-sm font-semibold text-foreground", children: t2("verify.title") }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-0.5", children: t2("verify.subtitle") })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground font-medium", children: t2("verify.networkLabel") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "token-chip text-xs self-start", children: t2(networkLabelKey(token)) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground italic", children: t2("verify.networkAutoDetected") })
        ] }),
        !isVerified && /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { onSubmit: handleSubmit, className: "space-y-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Label,
              {
                htmlFor: "txHash",
                className: "text-xs font-medium text-foreground",
                children: t2("verify.txHashLabel")
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Input,
              {
                id: "txHash",
                value: txHash,
                onChange: (e) => setTxHash(e.target.value),
                placeholder: t2("verify.txHashPlaceholder"),
                className: "font-mono text-xs",
                disabled: submitMutation.isPending || isPending,
                "data-ocid": "input-tx-hash",
                "aria-label": t2("verify.txHashLabel")
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              type: "submit",
              className: "w-full gap-2",
              disabled: !txHash.trim() || submitMutation.isPending || isPending,
              "data-ocid": "btn-verify-payment",
              children: submitMutation.isPending || isPending ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-4 h-4 animate-spin" }),
                t2("verify.verifying")
              ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "w-4 h-4" }),
                t2("verify.submitBtn")
              ] })
            }
          )
        ] }),
        verificationResult && /* @__PURE__ */ jsxRuntimeExports.jsx(VerificationStatus, { result: verificationResult })
      ]
    }
  );
}
function useStateMachine(initialState, machine) {
  return reactExports.useReducer((state, event) => {
    const nextState = machine[state][event];
    return nextState ?? state;
  }, initialState);
}
var SCROLL_AREA_NAME = "ScrollArea";
var [createScrollAreaContext] = createContextScope(SCROLL_AREA_NAME);
var [ScrollAreaProvider, useScrollAreaContext] = createScrollAreaContext(SCROLL_AREA_NAME);
var ScrollArea$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeScrollArea,
      type = "hover",
      dir,
      scrollHideDelay = 600,
      ...scrollAreaProps
    } = props;
    const [scrollArea, setScrollArea] = reactExports.useState(null);
    const [viewport, setViewport] = reactExports.useState(null);
    const [content, setContent] = reactExports.useState(null);
    const [scrollbarX, setScrollbarX] = reactExports.useState(null);
    const [scrollbarY, setScrollbarY] = reactExports.useState(null);
    const [cornerWidth, setCornerWidth] = reactExports.useState(0);
    const [cornerHeight, setCornerHeight] = reactExports.useState(0);
    const [scrollbarXEnabled, setScrollbarXEnabled] = reactExports.useState(false);
    const [scrollbarYEnabled, setScrollbarYEnabled] = reactExports.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setScrollArea(node));
    const direction = useDirection(dir);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaProvider,
      {
        scope: __scopeScrollArea,
        type,
        dir: direction,
        scrollHideDelay,
        scrollArea,
        viewport,
        onViewportChange: setViewport,
        content,
        onContentChange: setContent,
        scrollbarX,
        onScrollbarXChange: setScrollbarX,
        scrollbarXEnabled,
        onScrollbarXEnabledChange: setScrollbarXEnabled,
        scrollbarY,
        onScrollbarYChange: setScrollbarY,
        scrollbarYEnabled,
        onScrollbarYEnabledChange: setScrollbarYEnabled,
        onCornerWidthChange: setCornerWidth,
        onCornerHeightChange: setCornerHeight,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive.div,
          {
            dir: direction,
            ...scrollAreaProps,
            ref: composedRefs,
            style: {
              position: "relative",
              // Pass corner sizes as CSS vars to reduce re-renders of context consumers
              ["--radix-scroll-area-corner-width"]: cornerWidth + "px",
              ["--radix-scroll-area-corner-height"]: cornerHeight + "px",
              ...props.style
            }
          }
        )
      }
    );
  }
);
ScrollArea$1.displayName = SCROLL_AREA_NAME;
var VIEWPORT_NAME = "ScrollAreaViewport";
var ScrollAreaViewport = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, children, nonce, ...viewportProps } = props;
    const context = useScrollAreaContext(VIEWPORT_NAME, __scopeScrollArea);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref, context.onViewportChange);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "style",
        {
          dangerouslySetInnerHTML: {
            __html: `[data-radix-scroll-area-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-scroll-area-viewport]::-webkit-scrollbar{display:none}`
          },
          nonce
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          "data-radix-scroll-area-viewport": "",
          ...viewportProps,
          ref: composedRefs,
          style: {
            /**
             * We don't support `visible` because the intention is to have at least one scrollbar
             * if this component is used and `visible` will behave like `auto` in that case
             * https://developer.mozilla.org/en-US/docs/Web/CSS/overflow#description
             *
             * We don't handle `auto` because the intention is for the native implementation
             * to be hidden if using this component. We just want to ensure the node is scrollable
             * so could have used either `scroll` or `auto` here. We picked `scroll` to prevent
             * the browser from having to work out whether to render native scrollbars or not,
             * we tell it to with the intention of hiding them in CSS.
             */
            overflowX: context.scrollbarXEnabled ? "scroll" : "hidden",
            overflowY: context.scrollbarYEnabled ? "scroll" : "hidden",
            ...props.style
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: context.onContentChange, style: { minWidth: "100%", display: "table" }, children })
        }
      )
    ] });
  }
);
ScrollAreaViewport.displayName = VIEWPORT_NAME;
var SCROLLBAR_NAME = "ScrollAreaScrollbar";
var ScrollAreaScrollbar = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...scrollbarProps } = props;
    const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const { onScrollbarXEnabledChange, onScrollbarYEnabledChange } = context;
    const isHorizontal = props.orientation === "horizontal";
    reactExports.useEffect(() => {
      isHorizontal ? onScrollbarXEnabledChange(true) : onScrollbarYEnabledChange(true);
      return () => {
        isHorizontal ? onScrollbarXEnabledChange(false) : onScrollbarYEnabledChange(false);
      };
    }, [isHorizontal, onScrollbarXEnabledChange, onScrollbarYEnabledChange]);
    return context.type === "hover" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarHover, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "scroll" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarScroll, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "auto" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarAuto, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "always" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarVisible, { ...scrollbarProps, ref: forwardedRef }) : null;
  }
);
ScrollAreaScrollbar.displayName = SCROLLBAR_NAME;
var ScrollAreaScrollbarHover = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [visible, setVisible] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const scrollArea = context.scrollArea;
    let hideTimer = 0;
    if (scrollArea) {
      const handlePointerEnter = () => {
        window.clearTimeout(hideTimer);
        setVisible(true);
      };
      const handlePointerLeave = () => {
        hideTimer = window.setTimeout(() => setVisible(false), context.scrollHideDelay);
      };
      scrollArea.addEventListener("pointerenter", handlePointerEnter);
      scrollArea.addEventListener("pointerleave", handlePointerLeave);
      return () => {
        window.clearTimeout(hideTimer);
        scrollArea.removeEventListener("pointerenter", handlePointerEnter);
        scrollArea.removeEventListener("pointerleave", handlePointerLeave);
      };
    }
  }, [context.scrollArea, context.scrollHideDelay]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarAuto,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarScroll = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const isHorizontal = props.orientation === "horizontal";
  const debounceScrollEnd = useDebounceCallback(() => send("SCROLL_END"), 100);
  const [state, send] = useStateMachine("hidden", {
    hidden: {
      SCROLL: "scrolling"
    },
    scrolling: {
      SCROLL_END: "idle",
      POINTER_ENTER: "interacting"
    },
    interacting: {
      SCROLL: "interacting",
      POINTER_LEAVE: "idle"
    },
    idle: {
      HIDE: "hidden",
      SCROLL: "scrolling",
      POINTER_ENTER: "interacting"
    }
  });
  reactExports.useEffect(() => {
    if (state === "idle") {
      const hideTimer = window.setTimeout(() => send("HIDE"), context.scrollHideDelay);
      return () => window.clearTimeout(hideTimer);
    }
  }, [state, context.scrollHideDelay, send]);
  reactExports.useEffect(() => {
    const viewport = context.viewport;
    const scrollDirection = isHorizontal ? "scrollLeft" : "scrollTop";
    if (viewport) {
      let prevScrollPos = viewport[scrollDirection];
      const handleScroll = () => {
        const scrollPos = viewport[scrollDirection];
        const hasScrollInDirectionChanged = prevScrollPos !== scrollPos;
        if (hasScrollInDirectionChanged) {
          send("SCROLL");
          debounceScrollEnd();
        }
        prevScrollPos = scrollPos;
      };
      viewport.addEventListener("scroll", handleScroll);
      return () => viewport.removeEventListener("scroll", handleScroll);
    }
  }, [context.viewport, isHorizontal, send, debounceScrollEnd]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || state !== "hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": state === "hidden" ? "hidden" : "visible",
      ...scrollbarProps,
      ref: forwardedRef,
      onPointerEnter: composeEventHandlers(props.onPointerEnter, () => send("POINTER_ENTER")),
      onPointerLeave: composeEventHandlers(props.onPointerLeave, () => send("POINTER_LEAVE"))
    }
  ) });
});
var ScrollAreaScrollbarAuto = reactExports.forwardRef((props, forwardedRef) => {
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const { forceMount, ...scrollbarProps } = props;
  const [visible, setVisible] = reactExports.useState(false);
  const isHorizontal = props.orientation === "horizontal";
  const handleResize = useDebounceCallback(() => {
    if (context.viewport) {
      const isOverflowX = context.viewport.offsetWidth < context.viewport.scrollWidth;
      const isOverflowY = context.viewport.offsetHeight < context.viewport.scrollHeight;
      setVisible(isHorizontal ? isOverflowX : isOverflowY);
    }
  }, 10);
  useResizeObserver(context.viewport, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarVisible = reactExports.forwardRef((props, forwardedRef) => {
  const { orientation = "vertical", ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const thumbRef = reactExports.useRef(null);
  const pointerOffsetRef = reactExports.useRef(0);
  const [sizes, setSizes] = reactExports.useState({
    content: 0,
    viewport: 0,
    scrollbar: { size: 0, paddingStart: 0, paddingEnd: 0 }
  });
  const thumbRatio = getThumbRatio(sizes.viewport, sizes.content);
  const commonProps = {
    ...scrollbarProps,
    sizes,
    onSizesChange: setSizes,
    hasThumb: Boolean(thumbRatio > 0 && thumbRatio < 1),
    onThumbChange: (thumb) => thumbRef.current = thumb,
    onThumbPointerUp: () => pointerOffsetRef.current = 0,
    onThumbPointerDown: (pointerPos) => pointerOffsetRef.current = pointerPos
  };
  function getScrollPosition(pointerPos, dir) {
    return getScrollPositionFromPointer(pointerPos, pointerOffsetRef.current, sizes, dir);
  }
  if (orientation === "horizontal") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarX,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollLeft;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes, context.dir);
            thumbRef.current.style.transform = `translate3d(${offset}px, 0, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollLeft = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) {
            context.viewport.scrollLeft = getScrollPosition(pointerPos, context.dir);
          }
        }
      }
    );
  }
  if (orientation === "vertical") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarY,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollTop;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes);
            thumbRef.current.style.transform = `translate3d(0, ${offset}px, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollTop = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) context.viewport.scrollTop = getScrollPosition(pointerPos);
        }
      }
    );
  }
  return null;
});
var ScrollAreaScrollbarX = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarXChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "horizontal",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        bottom: 0,
        left: context.dir === "rtl" ? "var(--radix-scroll-area-corner-width)" : 0,
        right: context.dir === "ltr" ? "var(--radix-scroll-area-corner-width)" : 0,
        ["--radix-scroll-area-thumb-width"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.x),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.x),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollLeft + event.deltaX;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollWidth,
            viewport: context.viewport.offsetWidth,
            scrollbar: {
              size: ref.current.clientWidth,
              paddingStart: toInt(computedStyle.paddingLeft),
              paddingEnd: toInt(computedStyle.paddingRight)
            }
          });
        }
      }
    }
  );
});
var ScrollAreaScrollbarY = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarYChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "vertical",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        top: 0,
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: "var(--radix-scroll-area-corner-height)",
        ["--radix-scroll-area-thumb-height"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.y),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.y),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollTop + event.deltaY;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollHeight,
            viewport: context.viewport.offsetHeight,
            scrollbar: {
              size: ref.current.clientHeight,
              paddingStart: toInt(computedStyle.paddingTop),
              paddingEnd: toInt(computedStyle.paddingBottom)
            }
          });
        }
      }
    }
  );
});
var [ScrollbarProvider, useScrollbarContext] = createScrollAreaContext(SCROLLBAR_NAME);
var ScrollAreaScrollbarImpl = reactExports.forwardRef((props, forwardedRef) => {
  const {
    __scopeScrollArea,
    sizes,
    hasThumb,
    onThumbChange,
    onThumbPointerUp,
    onThumbPointerDown,
    onThumbPositionChange,
    onDragScroll,
    onWheelScroll,
    onResize,
    ...scrollbarProps
  } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, __scopeScrollArea);
  const [scrollbar, setScrollbar] = reactExports.useState(null);
  const composeRefs = useComposedRefs(forwardedRef, (node) => setScrollbar(node));
  const rectRef = reactExports.useRef(null);
  const prevWebkitUserSelectRef = reactExports.useRef("");
  const viewport = context.viewport;
  const maxScrollPos = sizes.content - sizes.viewport;
  const handleWheelScroll = useCallbackRef(onWheelScroll);
  const handleThumbPositionChange = useCallbackRef(onThumbPositionChange);
  const handleResize = useDebounceCallback(onResize, 10);
  function handleDragScroll(event) {
    if (rectRef.current) {
      const x = event.clientX - rectRef.current.left;
      const y = event.clientY - rectRef.current.top;
      onDragScroll({ x, y });
    }
  }
  reactExports.useEffect(() => {
    const handleWheel = (event) => {
      const element = event.target;
      const isScrollbarWheel = scrollbar == null ? void 0 : scrollbar.contains(element);
      if (isScrollbarWheel) handleWheelScroll(event, maxScrollPos);
    };
    document.addEventListener("wheel", handleWheel, { passive: false });
    return () => document.removeEventListener("wheel", handleWheel, { passive: false });
  }, [viewport, scrollbar, maxScrollPos, handleWheelScroll]);
  reactExports.useEffect(handleThumbPositionChange, [sizes, handleThumbPositionChange]);
  useResizeObserver(scrollbar, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollbarProvider,
    {
      scope: __scopeScrollArea,
      scrollbar,
      hasThumb,
      onThumbChange: useCallbackRef(onThumbChange),
      onThumbPointerUp: useCallbackRef(onThumbPointerUp),
      onThumbPositionChange: handleThumbPositionChange,
      onThumbPointerDown: useCallbackRef(onThumbPointerDown),
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          ...scrollbarProps,
          ref: composeRefs,
          style: { position: "absolute", ...scrollbarProps.style },
          onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
            const mainPointer = 0;
            if (event.button === mainPointer) {
              const element = event.target;
              element.setPointerCapture(event.pointerId);
              rectRef.current = scrollbar.getBoundingClientRect();
              prevWebkitUserSelectRef.current = document.body.style.webkitUserSelect;
              document.body.style.webkitUserSelect = "none";
              if (context.viewport) context.viewport.style.scrollBehavior = "auto";
              handleDragScroll(event);
            }
          }),
          onPointerMove: composeEventHandlers(props.onPointerMove, handleDragScroll),
          onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
            const element = event.target;
            if (element.hasPointerCapture(event.pointerId)) {
              element.releasePointerCapture(event.pointerId);
            }
            document.body.style.webkitUserSelect = prevWebkitUserSelectRef.current;
            if (context.viewport) context.viewport.style.scrollBehavior = "";
            rectRef.current = null;
          })
        }
      )
    }
  );
});
var THUMB_NAME = "ScrollAreaThumb";
var ScrollAreaThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...thumbProps } = props;
    const scrollbarContext = useScrollbarContext(THUMB_NAME, props.__scopeScrollArea);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || scrollbarContext.hasThumb, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaThumbImpl, { ref: forwardedRef, ...thumbProps }) });
  }
);
var ScrollAreaThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, style, ...thumbProps } = props;
    const scrollAreaContext = useScrollAreaContext(THUMB_NAME, __scopeScrollArea);
    const scrollbarContext = useScrollbarContext(THUMB_NAME, __scopeScrollArea);
    const { onThumbPositionChange } = scrollbarContext;
    const composedRef = useComposedRefs(
      forwardedRef,
      (node) => scrollbarContext.onThumbChange(node)
    );
    const removeUnlinkedScrollListenerRef = reactExports.useRef(void 0);
    const debounceScrollEnd = useDebounceCallback(() => {
      if (removeUnlinkedScrollListenerRef.current) {
        removeUnlinkedScrollListenerRef.current();
        removeUnlinkedScrollListenerRef.current = void 0;
      }
    }, 100);
    reactExports.useEffect(() => {
      const viewport = scrollAreaContext.viewport;
      if (viewport) {
        const handleScroll = () => {
          debounceScrollEnd();
          if (!removeUnlinkedScrollListenerRef.current) {
            const listener = addUnlinkedScrollListener(viewport, onThumbPositionChange);
            removeUnlinkedScrollListenerRef.current = listener;
            onThumbPositionChange();
          }
        };
        onThumbPositionChange();
        viewport.addEventListener("scroll", handleScroll);
        return () => viewport.removeEventListener("scroll", handleScroll);
      }
    }, [scrollAreaContext.viewport, debounceScrollEnd, onThumbPositionChange]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.div,
      {
        "data-state": scrollbarContext.hasThumb ? "visible" : "hidden",
        ...thumbProps,
        ref: composedRef,
        style: {
          width: "var(--radix-scroll-area-thumb-width)",
          height: "var(--radix-scroll-area-thumb-height)",
          ...style
        },
        onPointerDownCapture: composeEventHandlers(props.onPointerDownCapture, (event) => {
          const thumb = event.target;
          const thumbRect = thumb.getBoundingClientRect();
          const x = event.clientX - thumbRect.left;
          const y = event.clientY - thumbRect.top;
          scrollbarContext.onThumbPointerDown({ x, y });
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, scrollbarContext.onThumbPointerUp)
      }
    );
  }
);
ScrollAreaThumb.displayName = THUMB_NAME;
var CORNER_NAME = "ScrollAreaCorner";
var ScrollAreaCorner = reactExports.forwardRef(
  (props, forwardedRef) => {
    const context = useScrollAreaContext(CORNER_NAME, props.__scopeScrollArea);
    const hasBothScrollbarsVisible = Boolean(context.scrollbarX && context.scrollbarY);
    const hasCorner = context.type !== "scroll" && hasBothScrollbarsVisible;
    return hasCorner ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaCornerImpl, { ...props, ref: forwardedRef }) : null;
  }
);
ScrollAreaCorner.displayName = CORNER_NAME;
var ScrollAreaCornerImpl = reactExports.forwardRef((props, forwardedRef) => {
  const { __scopeScrollArea, ...cornerProps } = props;
  const context = useScrollAreaContext(CORNER_NAME, __scopeScrollArea);
  const [width, setWidth] = reactExports.useState(0);
  const [height, setHeight] = reactExports.useState(0);
  const hasSize = Boolean(width && height);
  useResizeObserver(context.scrollbarX, () => {
    var _a;
    const height2 = ((_a = context.scrollbarX) == null ? void 0 : _a.offsetHeight) || 0;
    context.onCornerHeightChange(height2);
    setHeight(height2);
  });
  useResizeObserver(context.scrollbarY, () => {
    var _a;
    const width2 = ((_a = context.scrollbarY) == null ? void 0 : _a.offsetWidth) || 0;
    context.onCornerWidthChange(width2);
    setWidth(width2);
  });
  return hasSize ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.div,
    {
      ...cornerProps,
      ref: forwardedRef,
      style: {
        width,
        height,
        position: "absolute",
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: 0,
        ...props.style
      }
    }
  ) : null;
});
function toInt(value) {
  return value ? parseInt(value, 10) : 0;
}
function getThumbRatio(viewportSize, contentSize) {
  const ratio = viewportSize / contentSize;
  return isNaN(ratio) ? 0 : ratio;
}
function getThumbSize(sizes) {
  const ratio = getThumbRatio(sizes.viewport, sizes.content);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const thumbSize = (sizes.scrollbar.size - scrollbarPadding) * ratio;
  return Math.max(thumbSize, 18);
}
function getScrollPositionFromPointer(pointerPos, pointerOffset, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const thumbCenter = thumbSizePx / 2;
  const offset = pointerOffset || thumbCenter;
  const thumbOffsetFromEnd = thumbSizePx - offset;
  const minPointerPos = sizes.scrollbar.paddingStart + offset;
  const maxPointerPos = sizes.scrollbar.size - sizes.scrollbar.paddingEnd - thumbOffsetFromEnd;
  const maxScrollPos = sizes.content - sizes.viewport;
  const scrollRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const interpolate = linearScale([minPointerPos, maxPointerPos], scrollRange);
  return interpolate(pointerPos);
}
function getThumbOffsetFromScroll(scrollPos, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const scrollbar = sizes.scrollbar.size - scrollbarPadding;
  const maxScrollPos = sizes.content - sizes.viewport;
  const maxThumbPos = scrollbar - thumbSizePx;
  const scrollClampRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const scrollWithoutMomentum = clamp(scrollPos, scrollClampRange);
  const interpolate = linearScale([0, maxScrollPos], [0, maxThumbPos]);
  return interpolate(scrollWithoutMomentum);
}
function linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos) {
  return scrollPos > 0 && scrollPos < maxScrollPos;
}
var addUnlinkedScrollListener = (node, handler = () => {
}) => {
  let prevPosition = { left: node.scrollLeft, top: node.scrollTop };
  let rAF = 0;
  (function loop() {
    const position = { left: node.scrollLeft, top: node.scrollTop };
    const isHorizontalScroll = prevPosition.left !== position.left;
    const isVerticalScroll = prevPosition.top !== position.top;
    if (isHorizontalScroll || isVerticalScroll) handler();
    prevPosition = position;
    rAF = window.requestAnimationFrame(loop);
  })();
  return () => window.cancelAnimationFrame(rAF);
};
function useDebounceCallback(callback, delay) {
  const handleCallback = useCallbackRef(callback);
  const debounceTimerRef = reactExports.useRef(0);
  reactExports.useEffect(() => () => window.clearTimeout(debounceTimerRef.current), []);
  return reactExports.useCallback(() => {
    window.clearTimeout(debounceTimerRef.current);
    debounceTimerRef.current = window.setTimeout(handleCallback, delay);
  }, [handleCallback, delay]);
}
function useResizeObserver(element, onResize) {
  const handleResize = useCallbackRef(onResize);
  useLayoutEffect2(() => {
    let rAF = 0;
    if (element) {
      const resizeObserver = new ResizeObserver(() => {
        cancelAnimationFrame(rAF);
        rAF = window.requestAnimationFrame(handleResize);
      });
      resizeObserver.observe(element);
      return () => {
        window.cancelAnimationFrame(rAF);
        resizeObserver.unobserve(element);
      };
    }
  }, [element, handleResize]);
}
var Root$1 = ScrollArea$1;
var Viewport = ScrollAreaViewport;
var Corner = ScrollAreaCorner;
function ScrollArea({
  className,
  children,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Root$1,
    {
      "data-slot": "scroll-area",
      className: cn("relative", className),
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Viewport,
          {
            "data-slot": "scroll-area-viewport",
            className: "focus-visible:ring-ring/50 size-full rounded-[inherit] transition-[color,box-shadow] outline-none focus-visible:ring-[3px] focus-visible:outline-1",
            children
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollBar, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Corner, {})
      ]
    }
  );
}
function ScrollBar({
  className,
  orientation = "vertical",
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbar,
    {
      "data-slot": "scroll-area-scrollbar",
      orientation,
      className: cn(
        "flex touch-none p-px transition-colors select-none",
        orientation === "vertical" && "h-full w-2.5 border-l border-l-transparent",
        orientation === "horizontal" && "h-2.5 flex-col border-t border-t-transparent",
        className
      ),
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        ScrollAreaThumb,
        {
          "data-slot": "scroll-area-thumb",
          className: "bg-border relative flex-1 rounded-full"
        }
      )
    }
  );
}
function isBackendError(result) {
  if (result.__kind__ === "err") return true;
  if (result.__kind__ === void 0 && "err" in result)
    return true;
  return false;
}
function extractError(result) {
  if (!isBackendError(result)) return null;
  return result.err ?? null;
}
function handleBackendError(err, navigate, locale) {
  const loc = detectLocale();
  if (!err) {
    ue.error(t(loc, "errors.generic"));
    return;
  }
  const kind = err.__kind__;
  switch (kind) {
    case "rate_limited":
      ue.error(t(loc, "errors.rateLimited"));
      break;
    case "invalid_input": {
      const msg = typeof err.invalid_input === "string" && err.invalid_input ? err.invalid_input : t(loc, "errors.invalidInput");
      ue.error(msg);
      break;
    }
    case "unauthorized":
      if (navigate) {
        ue.error(t(loc, "errors.anonymousNotAllowed"));
        navigate({ to: "/" });
      } else {
        ue.error(t(loc, "errors.unauthorized"));
      }
      break;
    case "escrow_error": {
      const msg = typeof err.escrow_error === "string" && err.escrow_error ? err.escrow_error : t(loc, "errors.escrowError");
      ue.error(msg);
      break;
    }
    case "banned":
      ue.error(t(loc, "errors.banned"));
      break;
    case "dispute_already_open":
      ue.error(t(loc, "errors.disputeAlreadyOpen"));
      break;
    default:
      ue.error(t(loc, "errors.generic"));
      break;
  }
}
function handleResultError(result, navigate, locale) {
  if (!isBackendError(result)) return false;
  const err = extractError(result);
  handleBackendError(err, navigate);
  return true;
}
function LightboxGallery({
  isOpen,
  imageUrl,
  onClose
}) {
  reactExports.useEffect(() => {
    if (!isOpen) return;
    const handler = (e) => {
      if (e.key === "Escape") onClose();
    };
    document.addEventListener("keydown", handler);
    return () => document.removeEventListener("keydown", handler);
  }, [isOpen, onClose]);
  if (!isOpen) return null;
  return reactDomExports.createPortal(
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "dialog",
      {
        open: true,
        className: "fixed inset-0 m-0 w-full h-full max-w-none max-h-none bg-black/90 z-50 flex items-center justify-center p-0 border-none",
        "data-ocid": "lightbox-gallery",
        "aria-label": "Image viewer",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              type: "button",
              className: "absolute top-4 right-4 p-2 rounded-full bg-black/60 text-white hover:bg-black/80 transition-colors",
              onClick: onClose,
              onKeyDown: (e) => e.key === "Enter" && onClose(),
              "aria-label": "Close",
              "data-ocid": "lightbox-gallery.close_button",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-5 h-5" })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "absolute inset-0",
              onClick: onClose,
              onKeyDown: (e) => e.key === "Enter" && onClose(),
              role: "button",
              tabIndex: -1,
              "aria-label": "Close lightbox"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "img",
            {
              src: imageUrl,
              alt: "Full size",
              className: "relative max-w-[90vw] max-h-[90vh] object-contain rounded-lg shadow-2xl z-10",
              onClick: (e) => e.stopPropagation(),
              onKeyDown: () => {
              }
            }
          )
        ]
      }
    ),
    document.body
  );
}
function LinkPreviewCard({ url }) {
  const { actor, isFetching } = useBackend();
  const [preview, setPreview] = reactExports.useState(null);
  const [loading, setLoading] = reactExports.useState(true);
  const [error, setError] = reactExports.useState(false);
  reactExports.useEffect(() => {
    if (!actor || isFetching) return;
    setLoading(true);
    setError(false);
    actor.getLinkPreview(url).then((res) => {
      if (res.__kind__ === "ok" && res.ok.title) {
        setPreview(res.ok);
      } else {
        setError(true);
      }
    }).catch(() => setError(true)).finally(() => setLoading(false));
  }, [url, actor, isFetching]);
  if (loading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "mt-1.5 p-2.5 rounded-lg border border-border bg-muted/30 flex gap-2.5 animate-pulse",
        "data-ocid": "link-preview-card",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 space-y-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-2.5 w-20 rounded" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-3 w-full rounded" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-2.5 w-3/4 rounded" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "w-16 h-14 rounded flex-shrink-0" })
        ]
      }
    );
  }
  if (error || !preview || !preview.title) return null;
  const title = preview.title ?? "";
  const description = preview.description ?? "";
  const siteName = preview.siteName ?? "";
  const imageUrl = preview.imageUrl ?? "";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "a",
    {
      href: url,
      target: "_blank",
      rel: "noopener noreferrer",
      className: "mt-1.5 flex gap-2.5 p-2.5 rounded-lg border border-border bg-muted/30 hover:bg-muted/50 transition-colors group block",
      "data-ocid": "link-preview-card",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0 space-y-0.5", children: [
          siteName && /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-[10px] text-muted-foreground flex items-center gap-1", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "w-2.5 h-2.5" }),
            siteName
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-semibold text-foreground leading-tight line-clamp-2 group-hover:text-primary transition-colors", children: title }),
          description && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[11px] text-muted-foreground line-clamp-2 leading-snug", children: description })
        ] }),
        imageUrl && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "img",
          {
            src: imageUrl,
            alt: title,
            className: "w-16 h-14 object-cover rounded flex-shrink-0",
            onError: (e) => {
              e.currentTarget.style.display = "none";
            }
          }
        )
      ]
    }
  );
}
function formatFileSize$1(bytes) {
  const n = Number(bytes);
  if (n < 1024) return `${n} B`;
  if (n < 1024 * 1024) return `${(n / 1024).toFixed(1)} KB`;
  return `${(n / (1024 * 1024)).toFixed(1)} MB`;
}
function FileIcon({ fileName }) {
  var _a;
  const ext = ((_a = fileName.split(".").pop()) == null ? void 0 : _a.toLowerCase()) ?? "";
  if (["pdf", "doc", "docx"].includes(ext)) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(FileText, { className: "w-5 h-5 text-primary flex-shrink-0" });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Paperclip, { className: "w-5 h-5 text-muted-foreground flex-shrink-0" });
}
function MediaAttachmentCard({ attachment }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "a",
    {
      href: attachment.url,
      download: attachment.fileName,
      target: "_blank",
      rel: "noopener noreferrer",
      className: "flex items-center gap-2.5 p-2.5 rounded-lg border border-border bg-muted/40 hover:bg-muted transition-colors group",
      "data-ocid": "media-attachment-card",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(FileIcon, { fileName: attachment.fileName }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-medium text-foreground truncate group-hover:text-primary transition-colors", children: attachment.fileName }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] text-muted-foreground", children: formatFileSize$1(attachment.fileSize) })
        ] })
      ]
    }
  );
}
const URL_REGEX = /(https?:\/\/[^\s]+)/g;
function formatTime(ts) {
  return new Date(Number(ts) / 1e6).toLocaleTimeString([], {
    hour: "2-digit",
    minute: "2-digit"
  });
}
function AttachmentItem({
  att,
  idx,
  onImageClick
}) {
  if (att.mimeType.startsWith("image/")) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        type: "button",
        className: "w-full p-0 border-0 bg-transparent cursor-pointer",
        onClick: () => onImageClick(att.url),
        "aria-label": `View ${att.fileName}`,
        "data-ocid": `chat-image-attachment.${idx + 1}`,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          "img",
          {
            src: att.url,
            alt: att.fileName,
            className: "max-h-48 sm:max-h-60 rounded-lg object-cover w-full"
          }
        )
      }
    );
  }
  if (att.mimeType.startsWith("video/")) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "video",
      {
        src: att.url,
        controls: true,
        className: "max-h-48 sm:max-h-60 rounded-lg w-full",
        "data-ocid": `chat-video-attachment.${idx + 1}`,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx("track", { kind: "captions" })
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(MediaAttachmentCard, { attachment: att });
}
function MessageBubble({
  message,
  isOwn,
  senderName
}) {
  const [lightboxUrl, setLightboxUrl] = reactExports.useState(null);
  const urlMatch = message.content.match(URL_REGEX);
  const firstUrl = urlMatch ? urlMatch[0] : null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: `flex flex-col gap-0.5 max-w-[85%] sm:max-w-[78%] ${isOwn ? "ml-auto items-end" : "items-start"}`,
        children: [
          !isOwn && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground px-1 font-medium", children: senderName }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: `px-3 py-2 sm:px-3.5 sm:py-2.5 rounded-2xl text-sm leading-relaxed break-words min-w-0 max-w-full ${isOwn ? "bg-primary text-primary-foreground rounded-br-sm" : "bg-muted text-foreground rounded-bl-sm"}`,
              children: [
                message.content && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "break-words whitespace-pre-wrap", children: message.content }),
                firstUrl && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: isOwn ? "opacity-90" : "", children: /* @__PURE__ */ jsxRuntimeExports.jsx(LinkPreviewCard, { url: firstUrl }) }),
                message.attachments.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mt-2 space-y-1.5", children: message.attachments.map((att, idx) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                  AttachmentItem,
                  {
                    att,
                    idx,
                    onImageClick: (url) => setLightboxUrl(url)
                  },
                  att.url
                )) }),
                message.attachments.length === 0 && message.attachmentUrl && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "a",
                  {
                    href: message.attachmentUrl,
                    target: "_blank",
                    rel: "noopener noreferrer",
                    className: "flex items-center gap-1.5 mt-1.5 text-xs underline opacity-80 hover:opacity-100 truncate max-w-[240px]",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Paperclip, { className: "w-3 h-3 flex-shrink-0" }),
                      "attachment"
                    ]
                  }
                )
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground px-1", children: formatTime(message.sentAt) })
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      LightboxGallery,
      {
        isOpen: lightboxUrl !== null,
        imageUrl: lightboxUrl ?? "",
        onClose: () => setLightboxUrl(null)
      }
    )
  ] });
}
let _pendingIdCounter = 0;
function formatFileSize(bytes) {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
function ChatPanel({
  tradeId,
  myPrincipal,
  unreadCount = 0,
  identity
}) {
  const { actor, isFetching } = useBackend();
  const { t: t2 } = useLocale();
  const qc = useQueryClient();
  const { uploadFile } = useUploadFile(identity);
  const { isVisible, justBecameVisible } = useVisiblePolling();
  const [text, setText] = reactExports.useState("");
  const [pendingFiles, setPendingFiles] = reactExports.useState([]);
  const [isDragging, setIsDragging] = reactExports.useState(false);
  const fileInputRef = reactExports.useRef(null);
  const bottomRef = reactExports.useRef(null);
  const dragCounterRef = reactExports.useRef(0);
  const {
    data: messages = [],
    isLoading,
    refetch: refetchMessages
  } = useQuery({
    queryKey: ["tradeMessages", tradeId.toString()],
    queryFn: () => actor.getTradeMessages(tradeId, 0n, 100n),
    enabled: !!actor && !isFetching,
    refetchInterval: isVisible ? 15e3 : false
  });
  reactExports.useEffect(() => {
    if (justBecameVisible) {
      void refetchMessages();
    }
  }, [justBecameVisible, refetchMessages]);
  reactExports.useEffect(() => {
    if (actor && !isFetching) {
      actor.markTradeAsRead(tradeId).catch(() => {
      });
    }
  }, [actor, isFetching, tradeId]);
  const msgCount = messages.length;
  const prevCountRef = reactExports.useRef(0);
  if (msgCount !== prevCountRef.current) {
    prevCountRef.current = msgCount;
    setTimeout(
      () => {
        var _a;
        return (_a = bottomRef.current) == null ? void 0 : _a.scrollIntoView({ behavior: "smooth" });
      },
      50
    );
  }
  const uploadFiles = reactExports.useCallback(
    async (files) => {
      const fileArray = Array.from(files);
      const currentCount = pendingFiles.filter(
        (f) => f.status !== "error"
      ).length;
      const canAdd = 10 - currentCount;
      if (canAdd <= 0) {
        ue.error(t2("chat.attachments.max"));
        return;
      }
      const toUpload = fileArray.slice(0, canAdd);
      const newEntries = toUpload.map((file) => {
        const id = ++_pendingIdCounter;
        const previewUrl = file.type.startsWith("image/") ? URL.createObjectURL(file) : void 0;
        return { id, file, progress: 0, status: "uploading", previewUrl };
      });
      setPendingFiles((prev) => [...prev, ...newEntries]);
      for (const entry of newEntries) {
        try {
          const url = await uploadFile(entry.file, (pct) => {
            setPendingFiles(
              (prev) => prev.map(
                (f) => f.id === entry.id ? { ...f, progress: pct } : f
              )
            );
          });
          const attachment = {
            url,
            mimeType: entry.file.type || "application/octet-stream",
            fileName: entry.file.name,
            fileSize: BigInt(entry.file.size)
          };
          setPendingFiles(
            (prev) => prev.map(
              (f) => f.id === entry.id ? { ...f, status: "done", progress: 100, attachment } : f
            )
          );
        } catch {
          setPendingFiles(
            (prev) => prev.map(
              (f) => f.id === entry.id ? { ...f, status: "error", progress: 0 } : f
            )
          );
          ue.error(t2("create.error.uploadFailed"));
        }
      }
    },
    [pendingFiles, uploadFile, t2]
  );
  const handleDragEnter = reactExports.useCallback((e) => {
    e.preventDefault();
    dragCounterRef.current += 1;
    if (e.dataTransfer.types.includes("Files")) setIsDragging(true);
  }, []);
  const handleDragLeave = reactExports.useCallback((e) => {
    e.preventDefault();
    dragCounterRef.current -= 1;
    if (dragCounterRef.current === 0) setIsDragging(false);
  }, []);
  const handleDragOver = reactExports.useCallback((e) => {
    e.preventDefault();
  }, []);
  const handleDrop = reactExports.useCallback(
    (e) => {
      e.preventDefault();
      dragCounterRef.current = 0;
      setIsDragging(false);
      if (e.dataTransfer.files.length > 0) {
        void uploadFiles(e.dataTransfer.files);
      }
    },
    [uploadFiles]
  );
  const handleFileChange = reactExports.useCallback(
    (e) => {
      if (e.target.files && e.target.files.length > 0) {
        void uploadFiles(e.target.files);
      }
      e.target.value = "";
    },
    [uploadFiles]
  );
  const removePending = reactExports.useCallback((id) => {
    setPendingFiles((prev) => {
      const entry = prev.find((f) => f.id === id);
      if (entry == null ? void 0 : entry.previewUrl) URL.revokeObjectURL(entry.previewUrl);
      return prev.filter((f) => f.id !== id);
    });
  }, []);
  const readyAttachments = pendingFiles.filter((f) => f.status === "done" && f.attachment).map((f) => f.attachment);
  const { mutate: sendMsg, isPending } = useMutation({
    mutationFn: () => actor.sendMessage(tradeId, text.trim(), readyAttachments),
    onSuccess: (res) => {
      if (handleResultError(
        res
      )) {
        return;
      }
      for (const f of pendingFiles) {
        if (f.previewUrl) URL.revokeObjectURL(f.previewUrl);
      }
      setText("");
      setPendingFiles([]);
      qc.invalidateQueries({ queryKey: ["tradeMessages", tradeId.toString()] });
    },
    onError: () => ue.error(t2("chat.sendError"))
  });
  const hasUploading = pendingFiles.some((f) => f.status === "uploading");
  const canSend = (text.trim().length > 0 || readyAttachments.length > 0) && !isPending && !hasUploading && !!actor;
  const atLimit = pendingFiles.filter((f) => f.status !== "error").length >= 10;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "flex flex-col h-full bg-card rounded-lg border border-border overflow-hidden relative",
      "data-ocid": "chat-panel",
      onDragEnter: handleDragEnter,
      onDragLeave: handleDragLeave,
      onDragOver: handleDragOver,
      onDrop: handleDrop,
      children: [
        isDragging && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 z-10 bg-primary/10 border-2 border-dashed border-primary rounded-lg flex items-center justify-center pointer-events-none", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Paperclip, { className: "w-8 h-8 text-primary mx-auto mb-2" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold text-primary", children: t2("chat.dropOverlay") })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between px-4 py-3 border-b border-border flex-shrink-0", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-semibold text-sm text-foreground", children: t2("chat.title") }),
          unreadCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              className: "status-badge-funded text-xs",
              "data-ocid": "chat-unread-badge",
              children: t2("chat.unread").replace("{count}", String(unreadCount))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "flex-1 px-3 py-2.5 sm:px-4 sm:py-3", children: isLoading ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-3", children: [1, 2, 3].map((i) => /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: `flex ${i % 2 === 0 ? "justify-end" : ""}`,
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-10 w-40 sm:w-48 rounded-2xl" })
          },
          i
        )) }) : messages.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "flex flex-col items-center justify-center h-32 text-center",
            "data-ocid": "chat-empty",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-3xl mb-2", children: "💬" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: t2("chat.empty") })
            ]
          }
        ) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5 sm:gap-2", children: [
          messages.map((msg) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            MessageBubble,
            {
              message: msg,
              isOwn: msg.sender.toString() === myPrincipal,
              senderName: msg.sender.toString().slice(0, 8)
            },
            msg.id.toString()
          )),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: bottomRef })
        ] }) }),
        pendingFiles.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "px-3 py-1.5 sm:px-4 sm:py-2 border-t border-border flex-shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "flex flex-wrap gap-1.5 sm:gap-2",
            "data-ocid": "chat-pending-attachments",
            children: pendingFiles.map((entry) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                className: "relative",
                "data-ocid": `chat-pending-attachment.${entry.id}`,
                children: [
                  entry.file.type.startsWith("image/") && entry.previewUrl ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-12 h-12 sm:w-16 sm:h-16 rounded-lg overflow-hidden border border-border relative", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "img",
                      {
                        src: entry.previewUrl,
                        alt: entry.file.name,
                        className: "w-full h-full object-cover"
                      }
                    ),
                    entry.status === "uploading" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-0 left-0 right-0 h-1 bg-border", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "div",
                      {
                        className: "h-full bg-primary transition-all",
                        style: { width: `${entry.progress}%` }
                      }
                    ) }),
                    entry.status === "error" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-destructive/60 flex items-center justify-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-white text-[10px]", children: "✕" }) })
                  ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1 px-2 py-1 rounded-lg border border-border bg-muted/40 min-w-0 max-w-[100px] sm:max-w-[120px]", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Paperclip, { className: "w-3 h-3 text-muted-foreground flex-shrink-0" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] text-foreground truncate", children: entry.file.name }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] text-muted-foreground", children: entry.status === "uploading" ? `${entry.progress}%` : formatFileSize(entry.file.size) })
                    ] }),
                    entry.status === "uploading" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-0 left-0 right-0 h-0.5 bg-border rounded", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "div",
                      {
                        className: "h-full bg-primary transition-all rounded",
                        style: { width: `${entry.progress}%` }
                      }
                    ) })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "button",
                    {
                      type: "button",
                      className: "absolute -top-1.5 -right-1.5 w-4 h-4 rounded-full bg-destructive text-destructive-foreground flex items-center justify-center hover:bg-destructive/80 transition-colors",
                      onClick: () => removePending(entry.id),
                      "aria-label": t2("chat.removeAttachment"),
                      "data-ocid": `chat-remove-attachment.${entry.id}`,
                      children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-2.5 h-2.5" })
                    }
                  )
                ]
              },
              entry.id
            ))
          }
        ) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            ref: fileInputRef,
            type: "file",
            accept: "image/*,video/*,.pdf,.doc,.docx,.txt",
            multiple: true,
            className: "hidden",
            onChange: handleFileChange,
            "data-ocid": "chat-file-input"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "px-2.5 py-2 sm:px-4 sm:py-3 border-t border-border flex items-center gap-1.5 sm:gap-2 flex-shrink-0", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              variant: "ghost",
              size: "icon",
              className: "flex-shrink-0 h-8 w-8 p-1.5",
              onClick: () => {
                var _a;
                return (_a = fileInputRef.current) == null ? void 0 : _a.click();
              },
              disabled: atLimit,
              "aria-label": t2("chat.attachment.ariaLabel"),
              "data-ocid": "chat-attach-btn",
              title: atLimit ? t2("chat.attachments.max") : void 0,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                Paperclip,
                {
                  className: `w-4 h-4 ${atLimit ? "text-muted-foreground/40" : pendingFiles.length > 0 ? "text-primary" : "text-muted-foreground"}`
                }
              )
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: text,
              onChange: (e) => setText(e.target.value),
              placeholder: t2("chat.placeholder"),
              className: "flex-1 h-9 text-sm",
              onKeyDown: (e) => {
                if (e.key === "Enter" && !e.shiftKey && canSend) {
                  e.preventDefault();
                  sendMsg();
                }
              },
              "data-ocid": "chat-message-input"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              size: "icon",
              className: "flex-shrink-0 h-9 w-9 min-w-[2.25rem]",
              disabled: !canSend,
              onClick: () => sendMsg(),
              "aria-label": t2("chat.send.ariaLabel"),
              "data-ocid": "chat-send-btn",
              children: isPending || hasUploading ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "animate-spin text-xs", children: "⏳" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Send, { className: "w-4 h-4" })
            }
          )
        ] })
      ]
    }
  );
}
var PROGRESS_NAME = "Progress";
var DEFAULT_MAX = 100;
var [createProgressContext] = createContextScope$1(PROGRESS_NAME);
var [ProgressProvider, useProgressContext] = createProgressContext(PROGRESS_NAME);
var Progress$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeProgress,
      value: valueProp = null,
      max: maxProp,
      getValueLabel = defaultGetValueLabel,
      ...progressProps
    } = props;
    if ((maxProp || maxProp === 0) && !isValidMaxNumber(maxProp)) {
      console.error(getInvalidMaxError(`${maxProp}`, "Progress"));
    }
    const max = isValidMaxNumber(maxProp) ? maxProp : DEFAULT_MAX;
    if (valueProp !== null && !isValidValueNumber(valueProp, max)) {
      console.error(getInvalidValueError(`${valueProp}`, "Progress"));
    }
    const value = isValidValueNumber(valueProp, max) ? valueProp : null;
    const valueLabel = isNumber(value) ? getValueLabel(value, max) : void 0;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(ProgressProvider, { scope: __scopeProgress, value, max, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive$1.div,
      {
        "aria-valuemax": max,
        "aria-valuemin": 0,
        "aria-valuenow": isNumber(value) ? value : void 0,
        "aria-valuetext": valueLabel,
        role: "progressbar",
        "data-state": getProgressState(value, max),
        "data-value": value ?? void 0,
        "data-max": max,
        ...progressProps,
        ref: forwardedRef
      }
    ) });
  }
);
Progress$1.displayName = PROGRESS_NAME;
var INDICATOR_NAME = "ProgressIndicator";
var ProgressIndicator = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeProgress, ...indicatorProps } = props;
    const context = useProgressContext(INDICATOR_NAME, __scopeProgress);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive$1.div,
      {
        "data-state": getProgressState(context.value, context.max),
        "data-value": context.value ?? void 0,
        "data-max": context.max,
        ...indicatorProps,
        ref: forwardedRef
      }
    );
  }
);
ProgressIndicator.displayName = INDICATOR_NAME;
function defaultGetValueLabel(value, max) {
  return `${Math.round(value / max * 100)}%`;
}
function getProgressState(value, maxValue) {
  return value == null ? "indeterminate" : value === maxValue ? "complete" : "loading";
}
function isNumber(value) {
  return typeof value === "number";
}
function isValidMaxNumber(max) {
  return isNumber(max) && !isNaN(max) && max > 0;
}
function isValidValueNumber(value, max) {
  return isNumber(value) && !isNaN(value) && value <= max && value >= 0;
}
function getInvalidMaxError(propValue, componentName) {
  return `Invalid prop \`max\` of value \`${propValue}\` supplied to \`${componentName}\`. Only numbers greater than 0 are valid max values. Defaulting to \`${DEFAULT_MAX}\`.`;
}
function getInvalidValueError(propValue, componentName) {
  return `Invalid prop \`value\` of value \`${propValue}\` supplied to \`${componentName}\`. The \`value\` prop must be:
  - a positive number
  - less than the value passed to \`max\` (or ${DEFAULT_MAX} if no \`max\` prop is set)
  - \`null\` or \`undefined\` if the progress is indeterminate.

Defaulting to \`null\`.`;
}
var Root = Progress$1;
var Indicator = ProgressIndicator;
function Progress({
  className,
  value,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Root,
    {
      "data-slot": "progress",
      className: cn(
        "bg-primary/20 relative h-2 w-full overflow-hidden rounded-full",
        className
      ),
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Indicator,
        {
          "data-slot": "progress-indicator",
          className: "bg-primary h-full w-full flex-1 transition-all",
          style: { transform: `translateX(-${100 - (value || 0)}%)` }
        }
      )
    }
  );
}
const REASON_KEYS = [
  {
    value: DisputeReason.item_differs,
    uiKey: "item_differs",
    labelKey: "dispute.reason.itemDiffers"
  },
  {
    value: DisputeReason.item_not_received,
    uiKey: "not_shipped",
    labelKey: "dispute.reason.notShipped"
  },
  {
    value: DisputeReason.other,
    uiKey: "payment_issue",
    labelKey: "dispute.reason.payment"
  },
  {
    value: DisputeReason.other,
    uiKey: "other",
    labelKey: "dispute.reason.other"
  }
];
const MAX_EVIDENCE = 5;
const MAX_DESC = 1e3;
const ACCEPTED_TYPES = "image/*,video/*,.pdf,.doc,.docx";
function formatBytes(bytes) {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
function DisputeModal({ open, onClose, tradeId }) {
  const { actor } = useBackend();
  const { identity } = useAuth();
  const { uploadFile } = useUploadFile(identity);
  const { t: tl } = useLocale();
  const qc = useQueryClient();
  const [reason, setReason] = reactExports.useState(REASON_KEYS[0].uiKey);
  const [description, setDescription] = reactExports.useState("");
  const [evidenceFiles, setEvidenceFiles] = reactExports.useState([]);
  const [isDragging, setIsDragging] = reactExports.useState(false);
  const nextId = reactExports.useRef(0);
  const evidenceInputRef = reactExports.useRef(null);
  const handleFiles = reactExports.useCallback(
    (files) => {
      const fileArray = Array.from(files);
      const remaining = MAX_EVIDENCE - evidenceFiles.length;
      if (remaining <= 0) {
        ue.warning(`Maximum ${MAX_EVIDENCE} evidence files allowed`);
        return;
      }
      const toUpload = fileArray.slice(0, remaining);
      for (const file of toUpload) {
        const id = nextId.current++;
        const previewUrl = file.type.startsWith("image/") ? URL.createObjectURL(file) : void 0;
        setEvidenceFiles((prev) => [
          ...prev,
          { id, file, progress: 0, status: "uploading", previewUrl }
        ]);
        uploadFile(file, (pct) => {
          setEvidenceFiles(
            (prev) => prev.map((e) => e.id === id ? { ...e, progress: pct } : e)
          );
        }).then((url) => {
          const attachment = {
            url,
            mimeType: file.type || "application/octet-stream",
            fileName: file.name,
            fileSize: BigInt(file.size)
          };
          setEvidenceFiles(
            (prev) => prev.map(
              (e) => e.id === id ? { ...e, progress: 100, status: "done", attachment } : e
            )
          );
        }).catch((err) => {
          const msg = err instanceof Error ? err.message : "Upload failed";
          setEvidenceFiles(
            (prev) => prev.map(
              (e) => e.id === id ? { ...e, status: "error", errorMessage: msg } : e
            )
          );
          ue.error(`Failed to upload evidence: ${msg}`);
        });
      }
    },
    [evidenceFiles.length, uploadFile]
  );
  const onDragOver = reactExports.useCallback((e) => {
    e.preventDefault();
    setIsDragging(true);
  }, []);
  const onDragLeave = reactExports.useCallback((e) => {
    e.preventDefault();
    setIsDragging(false);
  }, []);
  const onDrop = reactExports.useCallback(
    (e) => {
      e.preventDefault();
      setIsDragging(false);
      if (e.dataTransfer.files.length > 0) {
        handleFiles(e.dataTransfer.files);
      }
    },
    [handleFiles]
  );
  const removeEvidence = reactExports.useCallback((id) => {
    setEvidenceFiles((prev) => {
      const item = prev.find((e) => e.id === id);
      if (item == null ? void 0 : item.previewUrl) URL.revokeObjectURL(item.previewUrl);
      return prev.filter((e) => e.id !== id);
    });
  }, []);
  const { mutate, isPending } = useMutation({
    mutationFn: async () => {
      var _a;
      const backendReason = ((_a = REASON_KEYS.find((r) => r.uiKey === reason)) == null ? void 0 : _a.value) ?? DisputeReason.other;
      const res = await actor.openDispute(
        tradeId,
        backendReason,
        description.trim()
      );
      if (handleResultError(
        res
      )) {
        throw new Error("handled");
      }
      if (!("ok" in res)) throw new Error("Unexpected response");
      const disputeId = res.ok;
      const readyAttachments = evidenceFiles.filter((e) => e.status === "done" && e.attachment).map((e) => e.attachment);
      if (readyAttachments.length > 0 && actor) {
        try {
          await actor.addEvidence(disputeId, readyAttachments);
        } catch (evidenceErr) {
          console.error("[DisputeModal] addEvidence failed:", evidenceErr);
          ue.warning(
            "Dispute opened, but some evidence files could not be attached."
          );
        }
      }
    },
    onSuccess: () => {
      ue.success(tl("dispute.openedMessage"));
      qc.invalidateQueries({ queryKey: ["trade", tradeId.toString()] });
      onClose();
    },
    onError: (e) => {
      if (e.message === "handled") return;
      ue.error(`Failed to open dispute: ${e.message}`);
    }
  });
  const hasUploading = evidenceFiles.some((e) => e.status === "uploading");
  const canSubmit = description.trim().length >= 20 && description.length <= MAX_DESC && !isPending && !hasUploading && !!actor;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Dialog,
    {
      open,
      onOpenChange: (v) => !v && onClose(),
      "data-ocid": "dispute-modal",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs(DialogContent, { className: "max-w-lg max-h-[90vh] overflow-y-auto", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(DialogHeader, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-5 h-5 text-destructive" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(DialogTitle, { children: tl("dispute.form.title") })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground mt-1", children: tl("dispute.openedMessage") })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-5 py-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: "dispute-reason", children: tl("dispute.form.reason") }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Select, { value: reason, onValueChange: (v) => setReason(v), children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                SelectTrigger,
                {
                  id: "dispute-reason",
                  "data-ocid": "dispute-reason-select",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(SelectValue, {})
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(SelectContent, { children: REASON_KEYS.map((r) => /* @__PURE__ */ jsxRuntimeExports.jsx(SelectItem, { value: r.uiKey, children: tl(r.labelKey) }, r.uiKey)) })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: "dispute-desc", children: tl("dispute.form.description") }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "span",
                {
                  className: description.length > MAX_DESC ? "text-xs text-destructive" : "text-xs text-muted-foreground",
                  children: [
                    description.length,
                    "/",
                    MAX_DESC
                  ]
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Textarea,
              {
                id: "dispute-desc",
                placeholder: "Describe what happened in detail (min. 20 characters)…",
                value: description,
                onChange: (e) => setDescription(e.target.value),
                className: "resize-none",
                rows: 4,
                maxLength: MAX_DESC,
                "data-ocid": "dispute-description"
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { children: "Evidence Files" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-muted-foreground", children: [
              "Upload up to ",
              MAX_EVIDENCE,
              " photos, videos, or documents as evidence"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                ref: evidenceInputRef,
                type: "file",
                accept: ACCEPTED_TYPES,
                multiple: true,
                className: "sr-only",
                "data-ocid": "dispute-evidence-input",
                onChange: (e) => {
                  if (e.target.files && e.target.files.length > 0) {
                    handleFiles(e.target.files);
                    e.target.value = "";
                  }
                }
              }
            ),
            evidenceFiles.length < MAX_EVIDENCE && /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "button",
              {
                type: "button",
                "data-ocid": "dispute-evidence-dropzone",
                className: [
                  "w-full flex flex-col items-center justify-center gap-2 rounded-lg border-2 border-dashed p-6 cursor-pointer transition-colors text-left",
                  isDragging ? "border-primary bg-primary/5" : "border-border hover:border-primary/50 hover:bg-muted/30"
                ].join(" "),
                onClick: () => {
                  var _a;
                  return (_a = evidenceInputRef.current) == null ? void 0 : _a.click();
                },
                onDragOver,
                onDragLeave,
                onDrop,
                "aria-label": "Upload evidence files",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Upload, { className: "w-5 h-5 text-muted-foreground" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-sm text-muted-foreground text-center", children: [
                    "Drop evidence files here or",
                    " ",
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-primary font-medium", children: "click to select" })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: "Images, videos, PDF, DOC • max 25 MB each" })
                ]
              }
            ),
            evidenceFiles.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-2 mt-2", children: evidenceFiles.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                "data-ocid": "dispute-evidence-item",
                className: "flex items-start gap-3 rounded-lg border border-border bg-muted/20 p-2",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-shrink-0 w-12 h-12 rounded overflow-hidden bg-muted flex items-center justify-center", children: item.previewUrl ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "img",
                    {
                      src: item.previewUrl,
                      alt: item.file.name,
                      className: "w-full h-full object-cover"
                    }
                  ) : item.file.type.startsWith("image/") ? /* @__PURE__ */ jsxRuntimeExports.jsx(Image, { className: "w-5 h-5 text-muted-foreground" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(File, { className: "w-5 h-5 text-muted-foreground" }) }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium truncate", children: item.file.name }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: formatBytes(item.file.size) }),
                    item.status === "uploading" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-1.5", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Progress, { value: item.progress, className: "h-1.5" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-muted-foreground mt-0.5", children: [
                        item.progress,
                        "%"
                      ] })
                    ] }),
                    item.status === "done" && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-green-600 mt-1", children: "Uploaded ✓" }),
                    item.status === "error" && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-destructive mt-1 truncate", children: item.errorMessage ?? "Upload failed" })
                  ] }),
                  !isPending && /* @__PURE__ */ jsxRuntimeExports.jsx(
                    Button,
                    {
                      type: "button",
                      variant: "ghost",
                      size: "icon",
                      className: "flex-shrink-0 h-7 w-7",
                      onClick: () => removeEvidence(item.id),
                      "aria-label": "Remove evidence file",
                      children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3.5 h-3.5 text-muted-foreground" })
                    }
                  )
                ]
              },
              item.id
            )) })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(DialogFooter, { className: "gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              type: "button",
              variant: "outline",
              onClick: onClose,
              disabled: isPending,
              "data-ocid": "dispute-cancel-btn",
              children: tl("detail.cancel")
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              type: "button",
              variant: "destructive",
              disabled: !canSubmit,
              onClick: () => mutate(),
              "data-ocid": "dispute-submit-btn",
              children: isPending ? tl("jurors.submitting") : hasUploading ? tl("chat.uploading") : tl("dispute.form.title")
            }
          )
        ] })
      ] })
    }
  );
}
function getActiveStepIndex(status) {
  switch (status) {
    case TradeStatus.pending:
      return 0;
    case TradeStatus.funded:
      return 1;
    case TradeStatus.buyer_confirmed:
      return 3;
    case TradeStatus.payment_verified:
      return 4;
    case TradeStatus.complete:
      return 5;
    case TradeStatus.cancelled:
    case TradeStatus.refunded:
    case TradeStatus.disputed:
      return -1;
    default:
      return 0;
  }
}
function getStepState(stepIndex, status) {
  const isTerminal = [
    TradeStatus.cancelled,
    TradeStatus.refunded,
    TradeStatus.disputed
  ].includes(status);
  if (isTerminal) {
    const lastMeaningful = status === TradeStatus.buyer_confirmed ? 2 : 1;
    if (stepIndex < lastMeaningful) return "completed";
    return "upcoming";
  }
  const activeIdx = getActiveStepIndex(status);
  if (stepIndex < activeIdx) return "completed";
  if (stepIndex === activeIdx) return "active";
  return "upcoming";
}
function formatTs(ts) {
  if (!ts) return void 0;
  return new Date(Number(ts) / 1e6).toLocaleString();
}
function EscrowTimeline({
  status,
  createdAt,
  fundedAt,
  confirmedAt,
  completedAt
}) {
  const { t: t2 } = useLocale();
  const mainSteps = [
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(ShieldCheck, { className: "w-4 h-4" }),
      label: t2("trade.step.initiated"),
      description: t2("escrow.step.accepted.desc"),
      subText: void 0,
      timestamp: createdAt
    },
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Clock, { className: "w-4 h-4" }),
      label: t2("trade.step.awaitingPayment"),
      description: t2("escrow.step.pending.desc"),
      subText: t2("trade.hint.sendOffChain"),
      timestamp: fundedAt
    },
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Banknote, { className: "w-4 h-4" }),
      label: t2("trade.step.buyerConfirmed"),
      description: t2("escrow.step.funded.desc"),
      subText: void 0,
      timestamp: confirmedAt
    },
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Clock, { className: "w-4 h-4" }),
      label: t2("trade.step.awaitingSeller"),
      description: t2("escrow.step.buyer_confirmed.desc"),
      subText: t2("trade.hint.verifyOffChain"),
      timestamp: void 0
    },
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4" }),
      label: t2("trade.step.sellerConfirmed"),
      description: t2("escrow.step.buyer_confirmed.desc"),
      subText: void 0,
      timestamp: void 0
    },
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4" }),
      label: t2("trade.step.complete"),
      description: t2("escrow.step.complete.desc"),
      subText: void 0,
      timestamp: completedAt
    }
  ];
  const isTerminal = [
    TradeStatus.cancelled,
    TradeStatus.refunded,
    TradeStatus.disputed
  ].includes(status);
  const terminalMap = {
    [TradeStatus.disputed]: {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4" }),
      label: t2("escrow.terminal.disputed.label"),
      description: t2("escrow.terminal.disputed.desc"),
      cls: "text-destructive border-destructive bg-destructive/10"
    },
    [TradeStatus.refunded]: {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCcw, { className: "w-4 h-4" }),
      label: t2("escrow.terminal.refunded.label"),
      description: t2("escrow.terminal.refunded.desc"),
      cls: "text-chart-2 border-chart-2 bg-chart-2/10"
    },
    [TradeStatus.cancelled]: {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(CircleX, { className: "w-4 h-4" }),
      label: t2("escrow.terminal.cancelled.label"),
      description: t2("escrow.terminal.cancelled.desc"),
      cls: "text-muted-foreground border-muted-foreground/40 bg-muted/30"
    }
  };
  const STEP_KEYS = [
    "initiated",
    "awaitingPayment",
    "buyerConfirmed",
    "awaitingSeller",
    "sellerConfirmed",
    "complete"
  ];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative flex flex-col gap-0", "data-ocid": "escrow-timeline", children: [
    mainSteps.map((step, i) => {
      const state = getStepState(i, status);
      const isLast = i === mainSteps.length - 1;
      const dotCls = state === "completed" ? "bg-accent border-accent text-accent-foreground" : state === "active" ? "bg-primary border-primary text-primary-foreground ring-2 ring-primary/30" : "bg-muted border-border text-muted-foreground";
      const lineCls = state === "completed" || state === "active" && i > 0 ? "bg-accent/50" : "bg-border";
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center flex-shrink-0", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: `w-8 h-8 rounded-full border-2 flex items-center justify-center transition-all ${dotCls}`,
              children: step.icon
            }
          ),
          !isLast && !isTerminal && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: `w-0.5 h-8 mt-0 ${lineCls} transition-colors`
            }
          ),
          !isLast && isTerminal && state !== "upcoming" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-0.5 h-8 mt-0 bg-accent/50 transition-colors" }),
          !isLast && isTerminal && state === "upcoming" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-0.5 h-8 mt-0 bg-border" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "pb-4 pt-1 min-w-0", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "p",
            {
              className: `text-sm font-semibold leading-tight ${state === "active" ? "text-foreground" : state === "completed" ? "text-foreground/80" : "text-muted-foreground"}`,
              children: step.label
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-0.5", children: step.description }),
          step.subText && state === "active" && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-primary/80 mt-1 italic", children: step.subText }),
          step.timestamp && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-accent mt-1 font-mono", children: formatTs(step.timestamp) })
        ] })
      ] }, STEP_KEYS[i]);
    }),
    isTerminal && terminalMap[status] && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col items-center flex-shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          className: `w-8 h-8 rounded-full border-2 flex items-center justify-center ${terminalMap[status].cls}`,
          children: terminalMap[status].icon
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "pb-2 pt-1", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold text-foreground", children: terminalMap[status].label }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-0.5", children: terminalMap[status].description })
      ] })
    ] })
  ] });
}
function mapToUnifiedStatus(raw) {
  const lower = raw.toLowerCase();
  if (lower.includes("delivered") || lower.includes("вручено"))
    return "delivered";
  if (lower.includes("out_for_delivery") || lower.includes("кур'єр"))
    return "out_for_delivery";
  if (lower.includes("arrived") || lower.includes("на відділенні") || lower.includes("прибув"))
    return "arrived_at_branch";
  if (lower.includes("transit") || lower.includes("транзит") || lower.includes("в дорозі") || lower.includes("пересилається"))
    return "in_transit";
  if (lower.includes("return") || lower.includes("повернення"))
    return "returned";
  if (lower.includes("exception") || lower.includes("помилка"))
    return "exception";
  return "created";
}
const CARRIER_LABELS = {
  nova_poshta: "Nova Poshta",
  ukrposhta: "Ukrposhta",
  meest: "Meest Express",
  self_pickup: "Self Pickup",
  digital: "Digital Delivery"
};
const TIMELINE_STEPS = [
  "created",
  "in_transit",
  "arrived_at_branch",
  "out_for_delivery",
  "delivered"
];
function ShippingTracker({
  carrier,
  trackingNumber,
  tradeId
}) {
  const { t: t2 } = useLocale();
  const { actor, isFetching: actorFetching } = useBackend();
  const qc = useQueryClient();
  const prevUnifiedRef = reactExports.useRef(null);
  const {
    data: tracking,
    isFetching,
    isLoading,
    refetch
  } = useQuery({
    queryKey: ["trackingInfo", tradeId.toString()],
    queryFn: async () => {
      if (!actor) return null;
      const res = await actor.getUnifiedTrackingInfo(tradeId);
      if (res.__kind__ === "ok") {
        const info = res.ok;
        const unified = mapToUnifiedStatus(info.status);
        return {
          raw: info.status,
          unified,
          carrier: info.carrier,
          estimatedDelivery: info.estimatedDelivery ?? void 0,
          isDelivered: info.isDelivered,
          lastUpdated: /* @__PURE__ */ new Date()
        };
      }
      throw new Error("tracking_error");
    },
    enabled: !!actor && !actorFetching,
    refetchInterval: 3e4,
    staleTime: 2e4
  });
  reactExports.useEffect(() => {
    if (!tracking) return;
    const prev = prevUnifiedRef.current;
    if (prev !== null && prev !== tracking.unified) {
      if (tracking.isDelivered || tracking.unified === "delivered") {
        ue.success(t2("notification.shipping_delivered"));
        qc.invalidateQueries({ queryKey: ["trade"] });
      } else {
        ue.info(t2("notification.shipping_in_transit"));
      }
    }
    prevUnifiedRef.current = tracking.unified;
  }, [tracking, t2, qc]);
  const currentStepIdx = tracking ? TIMELINE_STEPS.indexOf(tracking.unified) : -1;
  const userLocale = typeof navigator !== "undefined" ? navigator.language : void 0;
  const showSkeleton = isLoading;
  const showRefreshing = isFetching && !isLoading;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "card-elevated p-4 space-y-4",
      "data-ocid": "tracking-status-card",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Truck, { className: "w-4 h-4 text-accent" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-semibold text-foreground", children: t2("shipping.trackingTitle") })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            showRefreshing && /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "w-3.5 h-3.5 text-muted-foreground animate-spin" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: "secondary", className: "text-xs font-mono", children: trackingNumber })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3", "data-ocid": "tracking-carrier", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-8 h-8 rounded-full bg-muted flex items-center justify-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Package, { className: "w-4 h-4 text-muted-foreground" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: t2("shipping.carrierLabel") }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-foreground", children: CARRIER_LABELS[carrier] ?? carrier })
          ] })
        ] }),
        showSkeleton ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", "data-ocid": "tracking-loading-state", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: t2("shipping.tracking.loading") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-4 w-3/4" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-3 w-1/2" })
        ] }) : tracking ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "flex items-center gap-1 py-2",
              "data-ocid": "tracking-timeline",
              children: TIMELINE_STEPS.map((step, idx) => {
                const isDone = idx <= currentStepIdx;
                const isCurrent = idx === currentStepIdx;
                return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "div",
                  {
                    className: "flex items-center flex-1 last:flex-none",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "div",
                        {
                          className: [
                            "w-5 h-5 rounded-full flex items-center justify-center shrink-0 transition-colors",
                            isCurrent ? "bg-primary text-primary-foreground" : isDone ? "bg-chart-1 text-primary-foreground" : "bg-muted text-muted-foreground"
                          ].join(" "),
                          children: isDone ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-3 h-3" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "w-1.5 h-1.5 rounded-full bg-current" })
                        }
                      ),
                      idx < TIMELINE_STEPS.length - 1 && /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "div",
                        {
                          className: [
                            "flex-1 h-0.5 mx-0.5 transition-colors",
                            isDone && idx < currentStepIdx ? "bg-chart-1" : "bg-muted"
                          ].join(" ")
                        }
                      )
                    ]
                  },
                  step
                );
              })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-muted/40 rounded-lg p-3 space-y-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
              tracking.unified === "delivered" ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4 text-chart-1" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Truck, { className: "w-4 h-4 text-accent" }),
              tracking.unified === "delivered" && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                Badge,
                {
                  variant: "secondary",
                  className: "text-xs bg-chart-1/10 text-chart-1 border-chart-1/30",
                  "data-ocid": "tracking-delivered-badge",
                  children: [
                    "✓ ",
                    tracking.unified.replace(/_/g, " ")
                  ]
                }
              ),
              tracking.unified !== "delivered" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-semibold text-foreground capitalize", children: tracking.unified.replace(/_/g, " ") })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground whitespace-pre-wrap leading-relaxed", children: tracking.raw }),
            tracking.estimatedDelivery && /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-muted-foreground", children: [
              "Est. delivery: ",
              tracking.estimatedDelivery
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-[10px] text-muted-foreground", children: [
              t2("shipping.lastUpdated"),
              " ",
              tracking.lastUpdated.toLocaleString(userLocale)
            ] })
          ] })
        ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "text-center py-4 text-sm text-muted-foreground",
            "data-ocid": "tracking-empty-state",
            children: t2("shipping.tracking.empty")
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            variant: "outline",
            size: "sm",
            className: "w-full gap-2",
            onClick: () => refetch(),
            disabled: isFetching || !actor,
            "data-ocid": "shipping-refresh-btn",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                RefreshCw,
                {
                  className: `w-3.5 h-3.5 ${isFetching ? "animate-spin" : ""}`
                }
              ),
              isFetching ? t2("shipping.refreshing") : t2("shipping.refreshButton")
            ]
          }
        )
      ]
    }
  );
}
function formatAmount(amount, token) {
  const n = Number(amount) / 1e8;
  return `${n.toFixed(n < 0.01 ? 6 : 4)} ${token}`;
}
function truncatePrincipal(p) {
  if (p.length <= 16) return p;
  return `${p.slice(0, 8)}…${p.slice(-6)}`;
}
function StatusBadge({ status }) {
  const { t: tl } = useLocale();
  const map = {
    pending: {
      labelKey: "trade.status.pending",
      cls: "status-badge-pending"
    },
    funded: { labelKey: "trade.status.funded", cls: "status-badge-funded" },
    awaiting_approval: {
      labelKey: "trade.status.awaiting_approval",
      cls: "status-badge-pending"
    },
    buyer_confirmed: {
      labelKey: "trade.status.buyer_confirmed",
      cls: "status-badge-confirmed"
    },
    payment_verified: {
      labelKey: "trade.status.payment_verified",
      cls: "inline-flex items-center gap-1 px-2 py-1 rounded-md text-xs font-medium bg-green-500/25 text-green-700 dark:text-green-200"
    },
    complete: {
      labelKey: "trade.status.complete",
      cls: "status-badge-confirmed"
    },
    refunded: {
      labelKey: "trade.status.refunded",
      cls: "status-badge-pending"
    },
    disputed: {
      labelKey: "trade.status.disputed",
      cls: "status-badge-dispute"
    },
    cancelled: {
      labelKey: "trade.status.cancelled",
      cls: "status-badge-pending"
    }
  };
  const { labelKey, cls } = map[status] ?? {
    labelKey: "trade.status.pending",
    cls: "status-badge-pending"
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: cls, children: tl(labelKey) });
}
function ListingSummaryCard({ listing }) {
  var _a;
  const { t: tl } = useLocale();
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "card-elevated p-4 space-y-3",
      "data-ocid": "listing-summary-card",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-full h-40 rounded-lg bg-muted overflow-hidden flex items-center justify-center", children: ((_a = listing.photos) == null ? void 0 : _a[0]) ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          "img",
          {
            src: listing.photos[0],
            alt: listing.title,
            className: "w-full h-full object-cover"
          }
        ) : /* @__PURE__ */ jsxRuntimeExports.jsx(Package, { className: "w-10 h-10 text-muted-foreground" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-semibold text-foreground text-sm leading-tight", children: listing.title }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-0.5", children: listing.category })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Separator, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground", children: tl("create.field.price") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "token-chip text-xs", children: formatAmount(listing.priceAmount, listing.priceToken) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground", children: tl("create.field.condition") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: "secondary", className: "text-xs", children: listing.condition })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground", children: tl("create.field.location") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-foreground", children: listing.location })
        ] })
      ]
    }
  );
}
const NETWORK_CONFIG = {
  USDT_TRC20: {
    accent: "text-green-700 dark:text-green-300",
    border: "border-green-500/40",
    bg: "bg-green-500/8"
  },
  USDT_BEP20: {
    accent: "text-yellow-700 dark:text-yellow-300",
    border: "border-yellow-500/40",
    bg: "bg-yellow-500/8"
  },
  USDT_ERC20: {
    accent: "text-blue-700 dark:text-blue-300",
    border: "border-blue-500/40",
    bg: "bg-blue-500/8"
  },
  USDC_ERC20: {
    accent: "text-indigo-700 dark:text-indigo-300",
    border: "border-indigo-500/40",
    bg: "bg-indigo-500/8"
  }
};
function NetworkInstructionPanel({ token }) {
  const { t: tl } = useLocale();
  const tokenKey = String(token);
  const cfg = NETWORK_CONFIG[tokenKey] ?? NETWORK_CONFIG.USDT_TRC20;
  const approved = ["USDT_TRC20", "USDT_BEP20", "USDT_ERC20", "USDC_ERC20"];
  if (!approved.includes(String(token))) return null;
  const titleKey = `trade.instructions.${tokenKey}.title`;
  const bodyKey = `trade.instructions.${tokenKey}.body`;
  const warnKey = `trade.instructions.${tokenKey}.warning`;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `rounded-lg border ${cfg.border} ${cfg.bg} p-4 space-y-2`,
      "data-ocid": "network-instruction-panel",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `flex items-center gap-2 ${cfg.accent}`, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Info, { className: "w-4 h-4 shrink-0" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold", children: tl(titleKey) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-foreground leading-relaxed", children: tl(bodyKey) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-2 pt-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 text-destructive shrink-0 mt-0.5" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-destructive", children: tl(warnKey) })
        ] })
      ]
    }
  );
}
function SellerReminderPanel({
  amount,
  token
}) {
  const { t: tl } = useLocale();
  const displayAmount = formatAmount(amount, token);
  const displayToken = String(token).replace(/_/g, "-");
  const body = tl("trade.sellerReminder.body").replace("{amount}", displayAmount).replace("{token}", displayToken);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "rounded-lg border border-amber-500/40 bg-amber-500/8 p-4 space-y-2",
      "data-ocid": "seller-reminder-panel",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-amber-700 dark:text-amber-300", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 shrink-0" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold", children: tl("trade.sellerReminder.title") })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-foreground leading-relaxed", children: body }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-2 pt-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 text-destructive shrink-0 mt-0.5" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-destructive", children: tl("trade.sellerReminder.warning") })
        ] })
      ]
    }
  );
}
function TradeActions({
  trade,
  isBuyer,
  onDisputeOpen
}) {
  const { actor, isFetching: actorFetching } = useBackend();
  const qc = useQueryClient();
  const { t: tl } = useLocale();
  const { isVisible } = useVisiblePolling();
  const navigate = useNavigate();
  const { data: trackingInfo } = useQuery({
    queryKey: ["trackingInfo", trade.id.toString()],
    queryFn: async () => {
      const res = await actor.getUnifiedTrackingInfo(trade.id);
      if (res.__kind__ === "ok") return res.ok;
      return null;
    },
    enabled: !!actor && !actorFetching && !isBuyer && (trade.status === TradeStatus.payment_verified || trade.status === TradeStatus.buyer_confirmed || trade.status === TradeStatus.funded),
    staleTime: 2e4,
    refetchInterval: isVisible ? 6e4 : false
  });
  async function doAction(fn, successMsg) {
    try {
      const res = await fn();
      if (res.__kind__ === "ok") {
        ue.success(successMsg);
        qc.invalidateQueries({ queryKey: ["trade", trade.id.toString()] });
      } else {
        handleResultError(
          res,
          navigate
        );
      }
    } catch {
      ue.error("Unexpected error");
    }
  }
  const s = trade.status;
  const isTerminal = [
    TradeStatus.complete,
    TradeStatus.refunded,
    TradeStatus.cancelled
  ].includes(s);
  if (isTerminal) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3 mt-4", "data-ocid": "trade-actions", children: [
    isBuyer && (s === TradeStatus.pending || s === TradeStatus.funded) && /* @__PURE__ */ jsxRuntimeExports.jsx(NetworkInstructionPanel, { token: trade.token }),
    !isBuyer && s === TradeStatus.buyer_confirmed && /* @__PURE__ */ jsxRuntimeExports.jsx(
      SellerReminderPanel,
      {
        amount: trade.amount,
        token: trade.token
      }
    ),
    isBuyer && s === TradeStatus.pending && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          className: "w-full gap-2",
          onClick: () => doAction(
            () => actor.confirmPaymentSent(trade.id),
            tl("trade.action.buyerSent")
          ),
          "data-ocid": "btn-payment-sent",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Banknote, { className: "w-4 h-4" }),
            tl("trade.action.buyerSent")
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground text-center px-2", children: tl("trade.hint.sendOffChain") })
    ] }),
    isBuyer && s === TradeStatus.buyer_confirmed && /* @__PURE__ */ jsxRuntimeExports.jsx(
      PaymentVerificationWidget,
      {
        tradeId: trade.id,
        token: trade.token
      }
    ),
    !isBuyer && (trackingInfo == null ? void 0 : trackingInfo.isDelivered) && (s === TradeStatus.payment_verified || s === TradeStatus.buyer_confirmed) && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Button,
      {
        className: "w-full gap-2 bg-green-600 hover:bg-green-700 text-white",
        onClick: () => doAction(
          () => actor.confirmPaymentReceived(trade.id),
          tl("trade.confirmDeliveryReleaseFunds")
        ),
        "data-ocid": "confirm-delivery-release-funds-btn",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4" }),
          tl("trade.confirmDeliveryReleaseFunds")
        ]
      }
    ),
    !isBuyer && s === TradeStatus.payment_verified && !(trackingInfo == null ? void 0 : trackingInfo.isDelivered) && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          className: "w-full gap-2",
          onClick: () => doAction(
            () => actor.confirmPaymentReceived(trade.id),
            tl("trade.action.sellerReceived")
          ),
          "data-ocid": "btn-payment-received-verified",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4" }),
            tl("trade.action.sellerReceived")
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground text-center px-2", children: tl("trade.hint.verifyOffChain") })
    ] }),
    !isBuyer && s === TradeStatus.funded && !(trackingInfo == null ? void 0 : trackingInfo.isDelivered) && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          className: "w-full gap-2",
          onClick: () => doAction(
            () => actor.confirmPaymentReceived(trade.id),
            tl("trade.action.sellerReceived")
          ),
          "data-ocid": "btn-payment-received",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4" }),
            tl("trade.action.sellerReceived")
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground text-center px-2", children: tl("trade.hint.verifyOffChain") })
    ] }),
    isBuyer && [
      TradeStatus.funded,
      TradeStatus.buyer_confirmed,
      TradeStatus.payment_verified
    ].includes(s) && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Button,
      {
        variant: "destructive",
        className: "w-full gap-2",
        onClick: onDisputeOpen,
        "data-ocid": "btn-open-dispute",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4" }),
          tl("dispute.form.title")
        ]
      }
    ),
    isBuyer && s === TradeStatus.disputed && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "flex items-center gap-2 rounded-lg border border-amber-500/40 bg-amber-500/10 px-3 py-2.5",
        "data-ocid": "dispute-already-opened",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 text-amber-600 dark:text-amber-400 shrink-0" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm text-amber-700 dark:text-amber-300", children: tl("dispute.alreadyOpened") })
        ]
      }
    ),
    isBuyer && s === TradeStatus.funded && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Button,
      {
        variant: "outline",
        className: "w-full gap-2",
        onClick: () => doAction(
          () => actor.requestRefund(trade.id),
          tl("trade.status.refunded")
        ),
        "data-ocid": "btn-request-refund",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCcw, { className: "w-4 h-4" }),
          tl("trade.status.refunded")
        ]
      }
    ),
    [TradeStatus.pending, TradeStatus.funded].includes(s) && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Button,
      {
        variant: "ghost",
        className: "w-full gap-2 text-muted-foreground hover:text-foreground",
        onClick: () => doAction(
          () => actor.proposeCancelTrade(trade.id),
          tl("trade.cancel")
        ),
        "data-ocid": "btn-propose-cancel",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(CircleX, { className: "w-4 h-4" }),
          tl("trade.cancel")
        ]
      }
    )
  ] });
}
const ACTIVE_SHIPPING_STATUSES = [
  TradeStatus.funded,
  TradeStatus.buyer_confirmed,
  TradeStatus.payment_verified,
  TradeStatus.complete
];
function getShippingProvider(trade) {
  const sel = trade.shippingSelection;
  if (Array.isArray(sel) && sel.length > 0) return sel[0].provider;
  if (sel && typeof sel === "object" && "provider" in sel)
    return sel.provider;
  return "nova_poshta";
}
function TTNControls({
  trade,
  isSeller,
  onManualTTNSaved
}) {
  const { actor } = useBackend();
  const qc = useQueryClient();
  const { t: tl } = useLocale();
  const [manualTTN, setManualTTN] = reactExports.useState("");
  const [savedManualTTN, setSavedManualTTN] = reactExports.useState("");
  const realTrackingNumber = trade.ttnNumber ?? null;
  const provider = getShippingProvider(trade);
  const ttnSectionKey = provider === "ukrposhta" ? "trade.ttnSection.ukrposhta" : provider === "meest" ? "trade.ttnSection.meest" : provider === "nova_poshta" ? "trade.ttnSection.nova_poshta" : "trade.ttnSection.default";
  const ttnStatusKey = Object.keys(trade.ttnCreationStatus)[0];
  const ttnIsPending = ttnStatusKey === "Pending";
  const ttnIsInProgress = ttnStatusKey === "InProgress";
  const ttnIsFailed = ttnStatusKey === "Failed";
  const ttnIsSuccess = ttnStatusKey === "Success";
  const isActiveForShipping = ACTIVE_SHIPPING_STATUSES.includes(trade.status);
  const createMutation = useMutation({
    mutationFn: () => {
      if (provider === "ukrposhta") return actor.createUkrposhtaTTN(trade.id);
      if (provider === "meest") return actor.createMeestTTN(trade.id);
      return actor.createNovaPoshtaTTN(trade.id);
    },
    onSuccess: (res) => {
      if ("ok" in res) {
        ue.success(tl("trade.ttnSuccess"));
        qc.invalidateQueries({ queryKey: ["trade", trade.id.toString()] });
      } else {
        const errObj = res.err;
        const errMsg = errObj.invalid_input ?? errObj.escrow_error ?? tl("trade.ttnFailed");
        ue.error(errMsg);
      }
    },
    onError: () => ue.error(tl("trade.ttnFailed"))
  });
  const retryMutation = useMutation({
    mutationFn: () => {
      if (provider === "ukrposhta")
        return actor.retryUkrposhtaTTNCreation(trade.id);
      if (provider === "meest") return actor.retryMeestTTNCreation(trade.id);
      return actor.retryTTNCreation(trade.id);
    },
    onSuccess: (res) => {
      if ("ok" in res) {
        ue.success(tl("trade.ttnSuccess"));
        qc.invalidateQueries({ queryKey: ["trade", trade.id.toString()] });
      } else {
        const errObj = res.err;
        const errMsg = errObj.invalid_input ?? errObj.escrow_error ?? tl("trade.ttnFailed");
        ue.error(errMsg);
      }
    },
    onError: () => ue.error(tl("trade.ttnFailed"))
  });
  if (!isSeller || !isActiveForShipping) {
    if (realTrackingNumber) {
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: "card-elevated p-4 space-y-2",
          "data-ocid": "ttn-number-section",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-semibold uppercase tracking-wide", children: tl(ttnSectionKey) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-mono text-foreground font-medium", children: realTrackingNumber })
          ]
        }
      );
    }
    return null;
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "card-elevated p-4 space-y-3", "data-ocid": "ttn-controls", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-semibold uppercase tracking-wide", children: tl(ttnSectionKey) }),
    realTrackingNumber && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "flex items-center gap-2 rounded-lg bg-muted/40 px-3 py-2",
        "data-ocid": "ttn-backend-number",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Package, { className: "w-3.5 h-3.5 text-muted-foreground shrink-0" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-mono text-foreground font-medium", children: realTrackingNumber })
        ]
      }
    ),
    ttnIsInProgress && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "flex items-center gap-2 text-muted-foreground",
        "data-ocid": "ttn-loading-state",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-4 h-4 animate-spin" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm", children: tl("trade.ttnCreating") })
        ]
      }
    ),
    ttnIsPending && !ttnIsSuccess && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Button,
      {
        size: "sm",
        className: "w-full gap-2",
        onClick: () => createMutation.mutate(),
        disabled: createMutation.isPending,
        "data-ocid": "ttn-create-button",
        children: [
          createMutation.isPending ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-4 h-4 animate-spin" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Package, { className: "w-4 h-4" }),
          tl("trade.ttnCreate")
        ]
      }
    ),
    ttnIsFailed && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Button,
      {
        size: "sm",
        variant: "outline",
        className: "w-full gap-2",
        onClick: () => retryMutation.mutate(),
        disabled: retryMutation.isPending,
        "data-ocid": "ttn-retry-button",
        children: [
          retryMutation.isPending ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-4 h-4 animate-spin" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCcw, { className: "w-4 h-4" }),
          tl("trade.ttnRetry")
        ]
      }
    ),
    !ttnIsInProgress && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2 pt-1", "data-ocid": "ttn-manual-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "label",
        {
          htmlFor: "ttn-manual-input",
          className: "text-xs text-muted-foreground",
          children: tl("shipping.manualTTN.label")
        }
      ),
      !savedManualTTN ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Input,
          {
            id: "ttn-manual-input",
            value: manualTTN,
            onChange: (e) => setManualTTN(e.target.value),
            placeholder: tl("shipping.manualTTN.placeholder"),
            className: "text-sm h-8",
            "data-ocid": "ttn-manual-input"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            size: "sm",
            variant: "secondary",
            className: "h-8 shrink-0",
            disabled: !manualTTN.trim(),
            onClick: () => {
              if (manualTTN.trim()) {
                setSavedManualTTN(manualTTN.trim());
                onManualTTNSaved == null ? void 0 : onManualTTNSaved(manualTTN.trim());
                ue.success(tl("trade.manualTTNSave"));
              }
            },
            "data-ocid": "ttn-manual-save-button",
            children: tl("trade.manualTTNSave")
          }
        )
      ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: "flex items-center justify-between",
          "data-ocid": "ttn-manual-saved",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-mono text-foreground", children: savedManualTTN }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "button",
                className: "text-xs text-muted-foreground hover:text-foreground",
                onClick: () => {
                  setSavedManualTTN("");
                  setManualTTN("");
                  onManualTTNSaved == null ? void 0 : onManualTTNSaved("");
                },
                children: "×"
              }
            )
          ]
        }
      )
    ] })
  ] });
}
function CopyButton({
  text,
  label,
  ocid
}) {
  const [copied, setCopied] = reactExports.useState(false);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "button",
    {
      type: "button",
      onClick: () => {
        navigator.clipboard.writeText(text).catch(() => {
        });
        setCopied(true);
        setTimeout(() => setCopied(false), 2e3);
      },
      className: "ml-2 p-1.5 rounded-md text-muted-foreground hover:text-foreground hover:bg-muted transition-colors shrink-0",
      "aria-label": label,
      "data-ocid": ocid,
      children: copied ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4 text-primary" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Copy, { className: "w-4 h-4" })
    }
  );
}
function DigitalDeliveryCard({
  delivery,
  isBuyer,
  tradeId,
  actor
}) {
  const { t: tl } = useLocale();
  const qc = useQueryClient();
  const [disputeOpen, setDisputeOpen] = reactExports.useState(false);
  const [disputeReason, setDisputeReason] = reactExports.useState(
    "item_differs"
  );
  const [countdown, setCountdown] = reactExports.useState({ hours: 0, minutes: 0 });
  const [isExpired, setIsExpired] = reactExports.useState(false);
  const intervalRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    function tick() {
      if (!delivery.inspectionDeadline) {
        setIsExpired(true);
        return;
      }
      const deadlineMs = Number(delivery.inspectionDeadline) / 1e6;
      const nowMs = Date.now();
      const diffMs = deadlineMs - nowMs;
      if (diffMs <= 0) {
        setIsExpired(true);
        if (intervalRef.current) clearInterval(intervalRef.current);
        if (actor) {
          actor.checkDigitalInspectionDeadline(tradeId).catch(() => {
          });
        }
      } else {
        const totalMinutes = Math.floor(diffMs / 6e4);
        setCountdown({
          hours: Math.floor(totalMinutes / 60),
          minutes: totalMinutes % 60
        });
        setIsExpired(false);
      }
    }
    tick();
    intervalRef.current = setInterval(tick, 6e4);
    return () => {
      if (intervalRef.current) clearInterval(intervalRef.current);
    };
  }, [delivery.inspectionDeadline, actor, tradeId]);
  const disputeMutation = useMutation({
    mutationFn: () => actor.openDigitalDispute(
      tradeId,
      disputeReason === "item_differs" ? "item_differs" : "other"
    ),
    onSuccess: (res) => {
      if ("ok" in res) {
        ue.success(tl("digital.dispute.button"));
        setDisputeOpen(false);
        qc.invalidateQueries({ queryKey: ["trade", tradeId.toString()] });
      } else {
        const errObj = res.err;
        ue.error(
          errObj.invalid_input ?? "Failed to open dispute"
        );
      }
    },
    onError: () => ue.error("Failed to open dispute")
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "card-elevated p-5 space-y-4 border-primary/20",
      "data-ocid": "digital-delivery-card",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "w-5 h-5 text-primary" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-sm font-semibold text-foreground", children: tl("digital.delivery.title") })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Separator, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-medium uppercase tracking-wide", children: tl("digital.delivery.fileUrl") }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center rounded-lg border border-border bg-muted/30 px-3 py-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "a",
              {
                href: delivery.fileUrl,
                target: "_blank",
                rel: "noopener noreferrer",
                className: "text-sm text-primary font-mono truncate flex-1 min-w-0 flex items-center gap-1.5 hover:underline",
                "data-ocid": "digital-delivery-file-link",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "w-3.5 h-3.5 shrink-0" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "truncate", children: delivery.fileUrl })
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              CopyButton,
              {
                text: delivery.fileUrl,
                label: tl("digital.delivery.copyUrl"),
                ocid: "digital-delivery-copy-url"
              }
            )
          ] })
        ] }),
        delivery.password && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-medium uppercase tracking-wide", children: tl("digital.delivery.password") }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center rounded-lg border border-border bg-muted/30 px-3 py-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-mono text-foreground flex-1 min-w-0 truncate", children: delivery.password }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              CopyButton,
              {
                text: delivery.password,
                label: tl("digital.delivery.copyPassword"),
                ocid: "digital-delivery-copy-password"
              }
            )
          ] })
        ] }),
        delivery.fileHash && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-medium uppercase tracking-wide", children: tl("digital.delivery.fileHash") }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center rounded-lg border border-border bg-muted/30 px-3 py-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-mono text-foreground flex-1 min-w-0 break-all", children: delivery.fileHash }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              CopyButton,
              {
                text: delivery.fileHash,
                label: "Copy hash",
                ocid: "digital-delivery-copy-hash"
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: tl("digital.delivery.hashNote") })
        ] }),
        isBuyer && isExpired && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "flex items-start gap-2 rounded-lg bg-muted/40 px-3 py-2.5",
            "data-ocid": "digital-inspection-expired",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-4 h-4 text-primary mt-0.5 shrink-0" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: tl("digital.delivery.inspectionExpired") })
            ]
          }
        ),
        isBuyer && !isExpired && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "flex items-start gap-2 rounded-lg bg-amber-500/10 border border-amber-500/20 px-3 py-2.5",
              "data-ocid": "digital-inspection-countdown",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Clock, { className: "w-4 h-4 text-amber-600 dark:text-amber-400 mt-0.5 shrink-0" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-amber-700 dark:text-amber-300", children: tl("digital.delivery.inspectionCountdown").replace("{hours}", String(countdown.hours)).replace("{minutes}", String(countdown.minutes)) })
              ]
            }
          ),
          !disputeOpen ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              variant: "destructive",
              size: "sm",
              className: "w-full gap-2",
              onClick: () => setDisputeOpen(true),
              "data-ocid": "digital-dispute-open-button",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(ShieldAlert, { className: "w-4 h-4" }),
                tl("digital.dispute.button")
              ]
            }
          ) : /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "rounded-lg border border-border bg-card p-4 space-y-3",
              "data-ocid": "digital-dispute-panel",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold text-foreground", children: tl("digital.dispute.title") }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col gap-2", children: [
                  {
                    value: "item_differs",
                    label: tl("digital.dispute.reasonItemDiffers")
                  },
                  {
                    value: "other",
                    label: tl("digital.dispute.reasonOther")
                  }
                ].map((r) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    type: "button",
                    onClick: () => setDisputeReason(r.value),
                    className: [
                      "text-left px-3 py-2.5 rounded-lg border text-sm transition-colors",
                      disputeReason === r.value ? "border-destructive bg-destructive/10 text-destructive" : "border-border bg-background text-foreground hover:border-destructive/40"
                    ].join(" "),
                    "data-ocid": `digital-dispute-reason-${r.value}`,
                    children: r.label
                  },
                  r.value
                )) }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    Button,
                    {
                      variant: "outline",
                      size: "sm",
                      className: "flex-1",
                      onClick: () => setDisputeOpen(false),
                      "data-ocid": "digital-dispute-cancel-button",
                      children: tl("detail.cancel")
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    Button,
                    {
                      variant: "destructive",
                      size: "sm",
                      className: "flex-1 gap-2",
                      onClick: () => disputeMutation.mutate(),
                      disabled: disputeMutation.isPending,
                      "data-ocid": "digital-dispute-submit-button",
                      children: [
                        disputeMutation.isPending && /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-3.5 h-3.5 animate-spin" }),
                        tl("digital.dispute.submit")
                      ]
                    }
                  )
                ] })
              ]
            }
          )
        ] })
      ]
    }
  );
}
function useJuryDeadlineCountdown(deadlineNs) {
  const [hoursLeft, setHoursLeft] = reactExports.useState(0);
  const [expired, setExpired] = reactExports.useState(false);
  reactExports.useEffect(() => {
    if (!deadlineNs) {
      setExpired(true);
      return;
    }
    function tick() {
      const deadlineMs = Number(deadlineNs) / 1e6;
      const diffMs = deadlineMs - Date.now();
      if (diffMs <= 0) {
        setExpired(true);
        setHoursLeft(0);
      } else {
        setExpired(false);
        setHoursLeft(Math.floor(diffMs / 36e5));
      }
    }
    tick();
    const id = setInterval(tick, 6e4);
    return () => clearInterval(id);
  }, [deadlineNs]);
  return { hoursLeft, expired };
}
function DisputeStatusCard({ dispute }) {
  var _a, _b, _c, _d;
  const { t: tl } = useLocale();
  const juryDeadline = (_a = dispute.jury) == null ? void 0 : _a.deadline;
  const { hoursLeft, expired } = useJuryDeadlineCountdown(juryDeadline);
  const buyerVotes = ((_b = dispute.jury) == null ? void 0 : _b.votes.filter((v) => v.vote === "buyerWins").length) ?? 0;
  const sellerVotes = ((_c = dispute.jury) == null ? void 0 : _c.votes.filter((v) => v.vote === "sellerWins").length) ?? 0;
  const totalJurors = ((_d = dispute.jury) == null ? void 0 : _d.jurors.length) ?? 0;
  if (dispute.status === DisputeStatus.resolved && dispute.resolution) {
    const res = dispute.resolution;
    const outcomeKey = res.outcome === ResolutionOutcome.buyer_wins ? "disputes.resolution.buyerWins" : res.outcome === ResolutionOutcome.seller_wins ? "disputes.resolution.sellerWins" : "disputes.resolution.split";
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "card-elevated p-4 space-y-3 border-primary/30",
        "data-ocid": "dispute-resolved-card",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(ShieldCheck, { className: "w-4 h-4 text-primary" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-semibold text-foreground", children: tl(outcomeKey) })
          ] }),
          res.notes && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "rounded-lg bg-muted/40 px-3 py-2.5 space-y-1", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-medium text-muted-foreground uppercase tracking-wide", children: tl("disputes.resolution.notes") }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-foreground", children: res.notes })
          ] }),
          dispute.resolvedAt && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: new Date(Number(dispute.resolvedAt) / 1e6).toLocaleString() })
        ]
      }
    );
  }
  if (dispute.status === DisputeStatus.escalated_to_admin) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "card-elevated p-4 space-y-2 border-destructive/30",
        "data-ocid": "dispute-escalated-card",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 text-destructive" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-semibold text-foreground", children: tl("disputes.jury.escalatedToAdmin") })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: tl("disputes.jury.noConsensus") })
        ]
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "card-elevated p-4 space-y-3 border-amber-500/30",
      "data-ocid": "dispute-jury-review-card",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Gavel, { className: "w-4 h-4 text-amber-600 dark:text-amber-400" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-semibold text-foreground", children: tl("disputes.jury.underReview") })
        ] }),
        juryDeadline && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: `flex items-center gap-1.5 text-xs ${expired ? "text-destructive" : "text-amber-600 dark:text-amber-400"}`,
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Clock, { className: "w-3.5 h-3.5 shrink-0" }),
              expired ? tl("disputes.jury.noConsensus") : tl("disputes.jury.deadlineCountdown").replace(
                "{{hours}}",
                String(hoursLeft)
              )
            ]
          }
        ),
        totalJurors > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs text-muted-foreground", children: tl("disputes.jury.voteTally").replace("{{buyer}}", String(buyerVotes)).replace("{{seller}}", String(sellerVotes)) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Badge, { variant: "outline", className: "text-[10px]", children: [
            buyerVotes + sellerVotes,
            "/",
            totalJurors
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-2 rounded-lg bg-amber-500/10 border border-amber-500/20 px-3 py-2.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-3.5 h-3.5 text-amber-600 dark:text-amber-400 mt-0.5 shrink-0" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-amber-700 dark:text-amber-300", children: tl("disputes.doNotRelease") })
        ] })
      ]
    }
  );
}
function TradeSkeleton() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-8 w-48" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-1 lg:grid-cols-3 gap-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-64 rounded-lg" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-96 rounded-lg" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-96 rounded-lg" })
    ] })
  ] });
}
function TradeDetailPage() {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m;
  const { id: tradeId } = useParams({ from: "/trades/$id" });
  const navigate = useNavigate();
  const { actor, isFetching } = useBackend();
  const { identity } = useInternetIdentity();
  const { t: tl } = useLocale();
  const [disputeOpen, setDisputeOpen] = reactExports.useState(false);
  const [mobileTab, setMobileTab] = reactExports.useState(
    "escrow"
  );
  const [manualTTNFromControls, setManualTTNFromControls] = reactExports.useState("");
  const { isVisible, justBecameVisible } = useVisiblePolling();
  const id = BigInt(tradeId);
  const { data: myProfile } = useQuery({
    queryKey: ["myProfile"],
    queryFn: () => actor.getMyProfile(),
    enabled: !!actor && !isFetching
  });
  const {
    data: trade,
    isLoading: tradeLoading,
    refetch: refetchTrade
  } = useQuery({
    queryKey: ["trade", tradeId],
    queryFn: () => actor.getTrade(id),
    enabled: !!actor && !isFetching,
    refetchInterval: isVisible ? 3e4 : false
  });
  const { data: listing } = useQuery({
    queryKey: ["listing", (_a = trade == null ? void 0 : trade.listing) == null ? void 0 : _a.toString()],
    queryFn: () => actor.getListing(trade.listing),
    enabled: !!actor && !isFetching && !!trade
  });
  const { data: unreadCounts = [] } = useQuery({
    queryKey: ["unreadCounts"],
    queryFn: () => actor.getUnreadCount(),
    enabled: !!actor && !isFetching
  });
  const myPrincipal = ((_b = myProfile == null ? void 0 : myProfile.id) == null ? void 0 : _b.toString()) ?? "";
  const isBuyerCheck = trade ? trade.buyer.toString() === myPrincipal : false;
  const { data: queriedDelivery, refetch: refetchDelivery } = useQuery({
    queryKey: ["digitalDelivery", ((_c = trade == null ? void 0 : trade.id) == null ? void 0 : _c.toString()) ?? ""],
    queryFn: async () => {
      const res = await actor.getDigitalDelivery(trade.id);
      if ("ok" in res) return res.ok;
      return null;
    },
    enabled: !!actor && !isFetching && !!trade && isBuyerCheck && trade.status === TradeStatus.complete
  });
  const { data: disputes, refetch: refetchDisputes } = useQuery({
    queryKey: ["tradeDisputes", tradeId],
    queryFn: () => actor.getDisputesByTrade(id),
    enabled: !!actor && !isFetching && !!trade && trade.status === TradeStatus.disputed,
    refetchInterval: isVisible ? 6e4 : false
  });
  reactExports.useEffect(() => {
    if (justBecameVisible) {
      void refetchTrade();
      void refetchDisputes();
      void refetchDelivery();
    }
  }, [justBecameVisible, refetchTrade, refetchDisputes, refetchDelivery]);
  if (tradeLoading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-screen bg-background", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "bg-card border-b border-border px-6 py-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-6 w-40" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "max-w-7xl mx-auto px-4 py-6", children: /* @__PURE__ */ jsxRuntimeExports.jsx(TradeSkeleton, {}) })
    ] });
  }
  if (!trade) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-screen bg-background flex flex-col items-center justify-center gap-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-5xl", children: "🔍" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-foreground font-semibold", children: tl("detail.notFound") }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", onClick: () => navigate({ to: "/trades" }), children: tl("trades.title") })
    ] });
  }
  const isBuyer = isBuyerCheck;
  const isSeller = trade.seller.toString() === myPrincipal;
  const counterparty = isBuyer ? trade.seller : trade.buyer;
  const unreadForTrade = ((_d = unreadCounts.find(([tid]) => tid === trade.id)) == null ? void 0 : _d[1]) ?? 0n;
  const hasShipping = (((_e = listing == null ? void 0 : listing.shippingMethods) == null ? void 0 : _e.length) ?? 0) > 0 && ((_g = (_f = listing == null ? void 0 : listing.shippingMethods) == null ? void 0 : _f[0]) == null ? void 0 : _g.carrier) !== ShippingCarrier.digital && ((_i = (_h = listing == null ? void 0 : listing.shippingMethods) == null ? void 0 : _h[0]) == null ? void 0 : _i.carrier) !== ShippingCarrier.self_pickup;
  const carrier = ((_k = (_j = listing == null ? void 0 : listing.shippingMethods) == null ? void 0 : _j[0]) == null ? void 0 : _k.carrier) ?? ShippingCarrier.nova_poshta;
  const realTrackingNumber = trade.ttnNumber ?? null;
  const effectiveTrackingNumber = realTrackingNumber ?? (manualTTNFromControls || null);
  const isDigitalTrade = !!(trade.digitalDelivery || ((_m = (_l = listing == null ? void 0 : listing.shippingMethods) == null ? void 0 : _l[0]) == null ? void 0 : _m.carrier) === ShippingCarrier.digital);
  const digitalDelivery = trade.digitalDelivery ?? queriedDelivery ?? null;
  const activeDispute = (disputes == null ? void 0 : disputes[0]) ?? null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-screen bg-background", "data-ocid": "trade-detail-page", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border-b border-border px-3 sm:px-6 py-3 sm:py-4 flex items-center gap-2 sm:gap-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: () => navigate({ to: "/trades" }),
          className: "text-muted-foreground hover:text-foreground transition-colors flex-shrink-0",
          "aria-label": "Back to trades",
          "data-ocid": "trade-back-btn",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowLeft, { className: "w-5 h-5" })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 min-w-0 sm:hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5 flex-wrap", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sm font-semibold text-foreground", children: [
          "#",
          trade.id.toString()
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBadge, { status: trade.status }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "token-chip text-xs", children: formatAmount(trade.amount, trade.token) })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hidden sm:block flex-1 min-w-0", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 flex-wrap", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("h1", { className: "text-base font-semibold text-foreground", children: [
            "Trade #",
            trade.id.toString()
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBadge, { status: trade.status })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-muted-foreground font-mono mt-0.5", children: [
          isBuyer ? tl("trades.tab.buying") : tl("trades.tab.selling"),
          ":",
          " ",
          truncatePrincipal(counterparty.toString())
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hidden sm:block flex-shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "token-chip text-xs", children: formatAmount(trade.amount, trade.token) }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-7xl mx-auto px-3 sm:px-4 py-3 sm:py-6", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hidden lg:grid grid-cols-[280px_1fr_320px] gap-5 h-[calc(100vh-10rem)]", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "overflow-y-auto space-y-4", children: [
          listing ? /* @__PURE__ */ jsxRuntimeExports.jsx(ListingSummaryCard, { listing }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "card-elevated p-4", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-40 w-full rounded-lg mb-3" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-4 w-3/4 mb-2" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-3 w-1/2" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "card-elevated p-4 space-y-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-semibold uppercase tracking-wide", children: isBuyer ? tl("detail.viewSellerProfile").replace("View ", "").replace(" profile", "") : "Buyer" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-mono text-foreground break-all", children: counterparty.toString() })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "overflow-y-auto space-y-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "card-elevated p-6", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-sm font-semibold text-foreground mb-5", children: tl("trade.step.initiated") }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              EscrowTimeline,
              {
                status: trade.status,
                createdAt: trade.createdAt,
                fundedAt: trade.fundedAt,
                confirmedAt: trade.confirmedAt,
                completedAt: trade.completedAt
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Separator, { className: "my-4" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              TradeActions,
              {
                trade,
                isBuyer,
                onDisputeOpen: () => setDisputeOpen(true)
              }
            )
          ] }),
          hasShipping && /* @__PURE__ */ jsxRuntimeExports.jsx(
            TTNControls,
            {
              trade,
              isSeller,
              onManualTTNSaved: setManualTTNFromControls
            }
          ),
          trade.status === TradeStatus.disputed && activeDispute && /* @__PURE__ */ jsxRuntimeExports.jsx(DisputeStatusCard, { dispute: activeDispute }),
          isDigitalTrade && trade.status === TradeStatus.complete && isBuyer && digitalDelivery && /* @__PURE__ */ jsxRuntimeExports.jsx(
            DigitalDeliveryCard,
            {
              delivery: digitalDelivery,
              isBuyer,
              tradeId: trade.id,
              actor
            }
          ),
          hasShipping && effectiveTrackingNumber && /* @__PURE__ */ jsxRuntimeExports.jsx(
            ShippingTracker,
            {
              carrier,
              trackingNumber: effectiveTrackingNumber,
              tradeId: id
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-full flex flex-col gap-2", children: [
          trade.status === TradeStatus.disputed && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "flex items-start gap-2 rounded-lg border border-amber-500/40 bg-amber-500/10 px-3 py-2.5",
              "data-ocid": "chat-dispute-banner",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 text-amber-600 dark:text-amber-400 shrink-0 mt-0.5" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-amber-700 dark:text-amber-300", children: tl("trade.dispute.chatBanner") })
              ]
            }
          ),
          (trade.status === TradeStatus.cancelled || trade.status === TradeStatus.refunded) && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "flex items-start gap-2 rounded-lg border border-border bg-muted/50 px-3 py-2.5",
              "data-ocid": "chat-cancelled-banner",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Info, { className: "w-4 h-4 text-muted-foreground shrink-0 mt-0.5" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: tl("trade.cancelled.chatBanner") })
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 min-h-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            ChatPanel,
            {
              tradeId: trade.id,
              myPrincipal,
              unreadCount: Number(unreadForTrade),
              identity: identity ?? void 0
            }
          ) })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "lg:hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Tabs,
        {
          value: mobileTab,
          onValueChange: (v) => setMobileTab(v),
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(TabsList, { className: "w-full mb-3", "data-ocid": "trade-mobile-tabs", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs(TabsTrigger, { value: "escrow", className: "flex-1 text-xs gap-1", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(ShieldCheck, { className: "w-3.5 h-3.5" }),
                tl("trade.step.initiated")
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                TabsTrigger,
                {
                  value: "chat",
                  className: "flex-1 text-xs relative gap-1",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "inline-block w-3.5 h-3.5", children: "💬" }),
                    tl("chat.title"),
                    Number(unreadForTrade) > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ml-1 status-badge-funded text-[10px] px-1.5 py-0", children: Number(unreadForTrade) })
                  ]
                }
              ),
              hasShipping && /* @__PURE__ */ jsxRuntimeExports.jsxs(TabsTrigger, { value: "shipping", className: "flex-1 text-xs gap-1", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Package, { className: "w-3.5 h-3.5" }),
                tl("detail.shipping")
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(TabsContent, { value: "escrow", className: "space-y-3", children: [
              listing && /* @__PURE__ */ jsxRuntimeExports.jsx(ListingSummaryCard, { listing }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "card-elevated p-3 sm:p-5", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-sm font-semibold text-foreground mb-4", children: tl("trade.step.initiated") }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  EscrowTimeline,
                  {
                    status: trade.status,
                    createdAt: trade.createdAt,
                    fundedAt: trade.fundedAt,
                    confirmedAt: trade.confirmedAt,
                    completedAt: trade.completedAt
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Separator, { className: "my-3" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  TradeActions,
                  {
                    trade,
                    isBuyer,
                    onDisputeOpen: () => setDisputeOpen(true)
                  }
                )
              ] }),
              isDigitalTrade && trade.status === TradeStatus.complete && isBuyer && digitalDelivery && /* @__PURE__ */ jsxRuntimeExports.jsx(
                DigitalDeliveryCard,
                {
                  delivery: digitalDelivery,
                  isBuyer,
                  tradeId: trade.id,
                  actor
                }
              ),
              trade.status === TradeStatus.disputed && activeDispute && /* @__PURE__ */ jsxRuntimeExports.jsx(DisputeStatusCard, { dispute: activeDispute })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              TabsContent,
              {
                value: "chat",
                className: "flex flex-col gap-2 h-[calc(100vh-13rem)]",
                children: [
                  trade.status === TradeStatus.disputed && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    "div",
                    {
                      className: "flex items-start gap-2 rounded-lg border border-amber-500/40 bg-amber-500/10 px-3 py-2.5 flex-shrink-0",
                      "data-ocid": "chat-dispute-banner-mobile",
                      children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 text-amber-600 dark:text-amber-400 shrink-0 mt-0.5" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-amber-700 dark:text-amber-300", children: tl("trade.dispute.chatBanner") })
                      ]
                    }
                  ),
                  (trade.status === TradeStatus.cancelled || trade.status === TradeStatus.refunded) && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    "div",
                    {
                      className: "flex items-start gap-2 rounded-lg border border-border bg-muted/50 px-3 py-2.5 flex-shrink-0",
                      "data-ocid": "chat-cancelled-banner-mobile",
                      children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(Info, { className: "w-4 h-4 text-muted-foreground shrink-0 mt-0.5" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: tl("trade.cancelled.chatBanner") })
                      ]
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 min-h-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                    ChatPanel,
                    {
                      tradeId: trade.id,
                      myPrincipal,
                      unreadCount: Number(unreadForTrade),
                      identity: identity ?? void 0
                    }
                  ) })
                ]
              }
            ),
            hasShipping && /* @__PURE__ */ jsxRuntimeExports.jsxs(TabsContent, { value: "shipping", className: "space-y-3", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                TTNControls,
                {
                  trade,
                  isSeller,
                  onManualTTNSaved: setManualTTNFromControls
                }
              ),
              effectiveTrackingNumber && /* @__PURE__ */ jsxRuntimeExports.jsx(
                ShippingTracker,
                {
                  carrier,
                  trackingNumber: effectiveTrackingNumber,
                  tradeId: id
                }
              )
            ] })
          ]
        }
      ) })
    ] }),
    disputeOpen && /* @__PURE__ */ jsxRuntimeExports.jsx(
      DisputeModal,
      {
        open: disputeOpen,
        onClose: () => setDisputeOpen(false),
        tradeId: trade.id
      }
    )
  ] });
}
export {
  TradeDetailPage as default
};
